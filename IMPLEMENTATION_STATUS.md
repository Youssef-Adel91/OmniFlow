# Implementation status — 2026-09-23

This is an engineering handoff, not a production-readiness declaration. The SRS remains the product specification. Preserve the existing architecture, naming, comments, and writing style when extending this work.

## Working tree

All work is uncommitted. Before this work, the user already had changes in `omniflow-backend/src/shared/core/config.py` (OpenAI-compatible provider settings) and `src/ai_engine/llm_orchestrator.py` (whitespace). Preserve those changes. No production deployment or real customer messages were performed.

## Implemented in this working tree

- Fixed the configuration syntax error and retained legacy model settings needed by existing workers.
- Bound tenant-facing repository sessions and SSE authentication to the authenticated user. Removed header-selected tenant context and rejected the reserved system tenant ID.
- Bound Clerk JWT verification to a configured/trusted issuer before fetching JWKS, with required claims and authorized-party checks.
- Added migration `0009_message_rls` to protect direct message queries through their owning conversation. The migration chain and actual RLS behavior passed under a non-superuser/non-BYPASSRLS role. The local application database was subsequently upgraded after a backup (see production hardening below).
- Corrected human takeover assignment, ownership checks, status normalization, and REST/SSE message mapping. Human sending currently supports WhatsApp only.
- Added a database-backed pending-message publisher scheduled by Celery Beat. Stable message IDs are shared with the dispatcher. Deployment requires Beat, a Celery worker, Kafka, and the outbound dispatcher.
- Changed outbound deduplication to record successful sends, permit retries after failures, and record Meta API acceptance as SENT rather than DELIVERED.
- Kafka commits only the processed record offset. A failed DLQ publish now terminates the consumer instead of allowing a later batch to acknowledge past the failed record.
- Inbound duplicate handling uses a savepoint and verifies the existing message before treating an integrity error as a duplicate; it no longer rolls back the entire transaction.
- Routing reads authoritative conversation/privacy state from the database. L0 replies reach the outgoing queue. LLM timeout escalation updates the conversation instead of publishing an incompatible event to the outgoing queue, preserving concurrent takeover/closure.
- Added backend/frontend Dockerfiles and ignore files, frontend standalone output, and build arguments in production Compose. Both images now build; container checks are recorded below. The complete production stack has not been deployed.
- Replaced the obsolete `/test` scratch page with its existing redirect behavior in Next configuration, fixing the production build's missing-root-layout failure.

## Verification

### Production hardening and container verification — 2026-09-23

- `/health` remains process liveness. `/health/ready` probes PostgreSQL, Redis and Kafka with bounded timeouts, returns 503 on dependency failure, and omits exception details. Production startup now requires these services and rejects a database role with superuser/BYPASSRLS privileges. The local endpoint returned all three checks `ok`.
- Production validation uses explicit exceptions, including under `python -O`, for debug mode, missing authentication, mock secrets/keys, unsafe CORS, and unconfigured voice output. Mock audio/storage and unfinished paid-report generation fail closed in production. Development report placeholders are no longer marked delivered. These guards do not complete the missing integrations.
- Production Compose separates migration-owner credentials from `APP_POSTGRES_USER` / `APP_POSTGRES_PASSWORD`. Deployment builds the maintenance profile, migrates before starting compute services, and provisions a nonprivileged application role. `provision-app-role.sql` was exercised twice in a disposable PostgreSQL container: grants, default grants, tenant RLS, and rejection of the admin role passed. No production roles were changed.
- Redis uses `noeviction`; MinIO initialization creates the configured private buckets; gateway access logging is disabled and Nginx access logs omit query strings. Global Docker image pruning was removed from deployment. Query-bearing error logs and complete storage ingress still need review.
- Updated Next.js to 16.3.6, React to 19, next-intl to 4, and affected dependencies. Converted route params to the asynchronous contract, migrated authentication to `proxy.ts`, and retained existing UI/layout conventions. Removed the random, non-persisted onboarding request number. `npm audit fix` and the Linux image's `npm ci` reported **0 vulnerabilities** at verification time.
- Frontend Docker build/runtime now use supported Node.js 24 LTS (verified runtime: 24.21.0), replacing Node.js 20 after its end of life. The final Linux build and TypeScript check passed on Node 24 and `npm ci` again reported zero known vulnerabilities. Support schedule: https://nodejs.org/en/about/previous-releases.
- Frontend: **12 tests passed**, production build/TypeScript passed, ESLint has **0 errors and 104 warnings**. The newly introduced `set-state-in-effect` performance rule is warning-level while existing loading effects remain; this is not a warning-free codebase. Clerk React 5 and ESLint 9 also emit upstream deprecation notices.
- Backend: **37 tests passed locally and inside the Linux image**, with no network access or real credentials in the container tests. Production guard tests also passed with Python optimization. `pip check` and imports of all three deployed Kafka workers plus Celery passed in the image. Docker verification tags are local, not published release tags.
- Backend image currently includes the complete ML/document-processing dependency set and is approximately **6.89 GB**; it installs CPU PyTorch instead of CUDA. Dependencies are still not locked. The final verification image reuses the successfully built dependency layer with the final application/Alembic source copied over it; `pip check`, all 37 tests, and offline migrations with reserved password characters passed again. A redundant clean dependency download was cancelled, not counted as a successful clean rebuild. Produce immutable release images from the final reviewed commit before deployment.
- The existing local database was at `9e62a0b74221`, missing the settings columns. Created a custom-format dump at ignored `omniflow-deploy/backups/postgres/local-before-readiness-20260923.dump`, then applied through `0009_message_rls`. The backup was subsequently restored successfully into a disposable, network-isolated PostgreSQL container, verifying the saved revision and row counts; that container and its volume were removed. Authenticated settings now load. Alembic escapes URL percent signs and disables asyncpg statement caches for PgBouncer.
- Re-ran the isolated full migration/RLS/inbox/report validator after the upgrade: passed, with its temporary transaction rolled back. Channel sends remain mocked in this validator.
- Fixed false-negative development health checks: PgBouncer now queries the application database instead of its restricted admin console; Qdrant no longer calls missing curl; Redpanda no longer depends on output spacing. All three local containers now report healthy, retaining their volumes.
- Backup now fails on missing Qdrant/MinIO or failed Qdrant listing and skips all retention pruning when any required backup failed. A network-isolated fake-Docker test verifies both failure cases and per-bucket destination separation. Default MinIO backup coverage now includes media, reports, temporary media, public assets and knowledge documents; `MINIO_SOURCE_BUCKETS` can override the list.
- Added the private knowledge bucket to production initialization. Executed the actual initialization commands against a disposable MinIO instance: all buckets were created, report/knowledge access remained private, and report versioning was enabled. Removed the temporary instance afterwards. This does not test public HTTPS storage ingress or a full object-store restore.
- Authenticated browser verification on the upgraded standalone frontend passed for inbox and settings. After a local restart, the frontend Docker image was started as `omniflow-frontend-validation` on **127.0.0.1:3000**; the existing Clerk session and synthetic inbox loaded from that image. Backend remains the local Python process on port 8000 with debug/access logging disabled. This mixed local setup is not the production stack.
- Removed hard-coded property recommendations/prices/match scores from the inbox context panel. It now shows an empty state until real conversation recommendations are wired in. The three previously inert quick-action buttons are disabled with an availability message; this does not implement those actions.

**Remaining launch gates:** real channel/worker end-to-end delivery, real embeddings and retrieval, payment/report generation, public storage URLs and bucket coverage, actual observability wiring (Sentry/OTel settings alone are not instrumentation), backup restore/off-host retention, dependency locking/scanning for Python, production domain/TLS/provider credentials, and staging acceptance/load tests. No public deployment or real customer communication occurred.

### Authenticated browser verification — 2026-09-23

- User signed in through Clerk. Initial 401s came from the sandboxed gateway failing to fetch the configured Clerk public signing keys. Restarting the local gateway with approved network access restored signature verification; no authentication bypass was added.
- Dashboard pages now wait for Clerk initialization before mounting data-fetching effects. A fresh browser reload showed the loading state followed by the authenticated inbox without the previous transient 401 banner.
- Fixed the Sidebar overview link from `/` (marketing page) to `/dashboard`; clicking it now shows real dashboard counts.
- Fixed legacy naive-UTC conversation timestamps in the API response. A newly created conversation previously displayed as three hours old; the browser now displays the correct elapsed minutes. Both naive UTC and offset-aware timestamps are regression-tested.
- Created one explicitly labelled local synthetic Web conversation (`local-browser-validation`, customer `عميل اختبار محلي`) with two test messages. It remains available for manual testing. No real channel send or customer communication was performed.
- Verified in the signed-in browser: list loading, message history, human takeover, return to AI, status persistence after reload, report empty state/revenue buckets, and dashboard counts. A second synthetic inbound message went through actual PostgreSQL persistence and Redis publication and appeared in the open conversation without a page reload.
- SQL echo now follows `APP_DEBUG` rather than automatically logging bound values in every development session. Current local gateway starts with debug/access logging disabled; replacing JWT query parameters in SSE with a stronger transport strategy remains follow-up work.
- Fresh verification: **27 backend tests**, **12 frontend tests**, TypeScript and Next production build passed. Existing lint warnings remain. Paid-report generation, payments, real channel delivery, and full-product acceptance are still outstanding.

### Reports and transport update — 2026-09-23

- Report detail and vault replies now obtain 15-minute signed download URLs. Signing validates the configured storage host/bucket and the owning tenant/customer object path. The UI requests a fresh URL on every download and does not fall back to an expired permanent URL.
- Report list exposes file availability as `ready` / `pending` without exposing permanent storage URLs. Frontend reads `price_sar`, revenue `series`, and the SRS report-type labels. Inclusive UTC date filters are implemented and reversed ranges return 422.
- `scripts/local_report_checks.py`, called by the isolated inbox validator, passed against PostgreSQL: both date boundaries, pagination, prices/status, daily revenue, cross-tenant 404, and no signing of inaccessible reports. All synthetic rows/schema/role were rolled back.
- `scripts/validate_local_transport.py` passed real Kafka/Redis publish, transient retry, successful replay deduplication, DLQ routing and offset commits. It creates/deletes only its own temporary topics and removes its completion keys from the cache Redis database. Producer idempotence is enabled for broker retries within a producer session.
- `scripts/validate_local_storage.py` previously passed a real MinIO round trip with synthetic text: signed GET succeeds, unsigned access is denied, and its temporary object is deleted. No report content or PDF generation is validated by that check.
- Current checks: **26 backend unit tests**, **12 frontend tests**, TypeScript, and the real PostgreSQL/transport checks passed. Backend unit discovery must use `.venv\\Scripts\\python.exe -m unittest discover -s tests -t . -q` so `tests/__init__.py` isolates live settings. Frontend tests can run with `node --test --test-isolation=none tests/*.test.cjs` when the sandbox disallows child processes.
- The local browser renders Clerk sign-in. Authenticated UI testing remains pending a user-provided test session; no authentication bypass was added.
- The updated Next production build passed (existing lint warnings remain). Gateway was restarted and checked for health, the new report parameters/status in OpenAPI, and rejection of unauthenticated report access.
- **Unfinished report generation:** the development placeholder is still plain UTF-8 text with a `.pdf` extension. Production now rejects it and it is no longer marked delivered. Payment integration and source-backed report content are not complete. Download/DTO fixes must not be described as completion of paid reports.

### Inbox reliability update — 2026-09-22

- SSE reconnect obtains a current authentication token, cancels obsolete asynchronous connection attempts, and uses bounded retry delays. Opening the stream refreshes the conversation list and active message cache.
- Unknown-conversation events trigger a list refresh. Repeated SSE messages do not increment the global unread badge twice; outgoing messages do not increment conversation unread counts.
- Optimistic replies use unique temporary IDs. REST confirmation removes an already-arrived SSE duplicate; failure removes only the corresponding pending reply. Forced history refresh merges concurrent SSE arrivals rather than overwriting them.
- Fresh verification: **9 frontend tests**, **23 backend tests**, TypeScript, and a full Next production build passed. Existing lint warnings remain. Browser/authenticated user-journey testing remains outstanding.
- The real PostgreSQL validator previously passed two-tenant RLS SELECT/UPDATE isolation using a non-superuser/non-BYPASSRLS role, same-phone separation, duplicate inbound persistence, takeover, outbox rollback/retry, and return to AI. Everything ran inside a temporary schema and transaction that was rolled back.
- After Docker became available, `.venv\Scripts\python.exe scripts/validate_local_migrations.py --via-pgbouncer --inbox` passed the full migration chain and the real database checks, including channel-send failure/retry, durable SENT state, completed-send replay suppression, cancelling an AI reply after takeover, and return to AI. Transport calls use mocks; this is not a live WhatsApp send. The temporary schema, role, and all synthetic data were rolled back.

### Groq integration added after the initial audit

- User supplied a Groq key. It is stored only in ignored `omniflow-backend/.env`; never copy it into this document, examples, logs, or Git.
- Shared provider configuration now connects both the gateway orchestrator and the existing invocation worker to the selected OpenAI-compatible service. Existing worker class/response contracts are retained.
- Local provider is `groq`, endpoint `https://api.groq.com/openai/v1`. ROUTER/L1 use `openai/gpt-oss-20b`; L2/L3 use `openai/gpt-oss-120b`. No fallback to another provider is configured.
- Both models are listed in Groq's free-plan limits at https://console.groq.com/docs/rate-limits. Account billing tier cannot be inferred from an API key; free usage depends on the account plan and quotas. `LLM_FREE_ONLY` specifically validates OpenRouter `:free` routes and is false for Groq; it is not a Groq billing control.
- Live synthetic requests to both models passed. `scripts/smoke_llm.py --application` also passed the gateway classifier, gateway response, and worker response using supplied synthetic property context. This did not send channel messages or use customer data.
- Backend suite now passes **23 tests**, including provider key isolation, model overrides, OpenRouter free-route validation, reasoning output handling, and worker history/context/token metadata.
- Worker startup skips vector retrieval when no real embedding key exists, logging `rag_unavailable_no_embedding_provider`. This avoids treating the legacy random-vector mock as genuine RAG. Live document retrieval, speech, and image processing are not covered by these chat tests.
- Restart already-running gateway/worker processes to load the new environment. No background workers were started by the smoke tests.

- Backend: ` .venv\Scripts\python.exe -m unittest tests.test_delivery tests.test_security -q` — **18 tests passed**. These exercise real application functions with mocked external boundaries; they do not establish live database RLS or full delivery integration.
- Frontend: `node --test tests/inbox-contract.test.cjs` — **3 tests passed**.
- Frontend: TypeScript check and full Next production build passed. ESLint warnings remain.
- `git diff --check` passed, with Git line-ending conversion warnings.
- Local isolated migration validator: `.venv\Scripts\python.exe scripts/validate_local_migrations.py --via-pgbouncer` — **full chain passed through `0009_message_rls`**, and the temporary transaction was rolled back. The script refuses remote database hosts. Alembic accepts a supplied connection for this validation.
- Database diagnosis: project credentials match Docker configuration, and a real query through PgBouncer on 6432 succeeds. Windows has a native `postgres` process listening on IPv4 port 5432 alongside Docker's listener. Direct host connections fail authentication; do not conclude the Docker database is down or change its password. No services were stopped and no database credentials were changed.

### Secret-exposure and legacy-auth check — 2026-09-23

- Searched full `git log --all -p` (this repo has a single squashed commit, "Initial commit for OmniFlow") for real Gemini/Meta credential values and for the literal token prefixes present in the local untracked `.env` (`EAAYni...`, `AQ.Ab8...`). No matches: every `GEMINI_API_KEY=`/`*_ACCESS_TOKEN=` line ever committed is a `<REQUIRED>` placeholder, and `.env`/`.env.local` were never tracked. **This means the exposed-secret concern from the prior audit does not apply to this repo's Git history as it exists now** — there is nothing to purge with `git filter-repo`/BFG, and no force-push is needed. This does not confirm whether the real keys were exposed elsewhere (a different prior repo state, chat, CI logs) before this history was squashed.
- The real Gemini and Meta Instagram credential values still sit in the untracked local `omniflow-backend/.env`. Per the user's own account these were exposed previously — **rotating them at the Google AI Studio / Meta developer consoles is a provider-side action only the account owner can perform; still needs to be done regardless of the clean git history.**
- Added `.pre-commit-config.yaml` (gitleaks hook) at the repo root to catch secrets before they're committed going forward. Not yet activated — requires `pip install pre-commit && pre-commit install` once, which needs to be run by whoever has write access to this checkout (not run by this session).
- Confirmed item 2 (legacy local-JWT paths) is already fully done in this working tree: `POST /login`, `/register`, `/refresh` in [auth.py](omniflow-backend/src/gateway/routers/auth.py) return HTTP 410 Gone and mint nothing; `PATCH /tenants/onboarding` in [tenants.py](omniflow-backend/src/gateway/routers/tenants.py) never issues a token (only updates `onboarding_status`); `DEMO_CREDENTIALS` does not exist anywhere in the frontend; the `TestBotSimulator`/`localhost:8000` widget is fully removed from [inbox/page.tsx](omniflow-frontend/src/app/[locale]/(dashboard)/inbox/page.tsx), leaving only an explanatory comment. No code changes were needed for item 2.

### Real PDF report generation — 2026-09-23

- Replaced the placeholder in [`vault_worker/generator.py`](omniflow-backend/src/ai_workers/vault_worker/generator.py) (was `f"OmniFlow AI Report: ...".encode("utf-8")` saved with a `.pdf` extension) with a genuine PDF built by the new [`pdf_builder.py`](omniflow-backend/src/ai_workers/vault_worker/pdf_builder.py) module, populated with real data pulled from the database at generation time: the tenant's `business_name`, the customer's `display_name`/`unified_phone` (queried live via `get_tenant_session`), the price paid, the transaction reference, the report type, and the generation timestamp. Any additional metadata keys on the payment event (e.g. a future deed number) render as extra report fields without code changes.
- **This went through three real, user-caught rounds before landing — recorded in full because each round found a genuine defect the previous self-check missed, not a flaky retest:**
  1. **First version (fpdf2 + manual `arabic_reshaper`/`python-bidi`):** self-check only verified PDF structural validity (header/trailer/size), never what was actually on the page. User caught it visually: Arabic letters rendered disconnected, and every row after the tenant name was missing from the page entirely (present in the raw byte stream, off-canvas — `multi_cell`'s default `new_x=XPos.RIGHT` leaves the cursor at the right edge of the previous line instead of resetting to the left margin).
  2. **Second version (fpdf2 + real HarfBuzz shaping via `set_text_shaping()`, cursor bug fixed):** fixed both visual bugs — verified by rasterizing and eyeballing a full realistic report. User then asked for the specific, correct test: does the PDF's actual **text layer** (not the picture of it) survive `pdftotext`/select-all? It did not. Investigation (downloaded real poppler `pdftotext`, installed `pikepdf`/`pdfminer.six`/`uharfbuzz` for direct inspection) found a real, reproducible fpdf2 bug: `TTFFont.shape_text` builds a HarfBuzz-cluster-to-source-character map and `.pop()`s each cluster the first time a glyph consumes it, so any glyph sharing a cluster with another (routine for Arabic — e.g. a base+mark pair) gets silently dropped from the PDF's `/ToUnicode` CMap. Confirmed against fpdf2 2.8.8 **and** an unreleased git-master build (commit `4287c924`, which does fix a related-but-different bug — [py-pdf/fpdf2#1952](https://github.com/py-pdf/fpdf2/issues/1952), the 100-entries-per-`bfchar`-block limit — but not this one). Effect: ~15-20% of glyphs on a page this size render perfectly on screen but are unextractable — garbled or missing on copy/paste, search, or a screen reader. A post-processing patch (`pikepdf`-based, filling the gaps with an explicit `U+FFFD` per missing code) reduced but did not eliminate the problem.
  3. **Final version:** rather than keep patching fpdf2/HarfBuzz internals, switched the whole PDF backend to **WeasyPrint** (HTML/CSS → PDF via Pango). Independently re-verified with real `pdftotext` (poppler, downloaded and run directly — not a Python wrapper) and `pdfminer.six` (a spec-literal ToUnicode-CMap extractor, unlike PyMuPDF which appears to reverse-engineer text from the embedded font directly and therefore doesn't actually exercise the `/ToUnicode` CMap this check cares about): **zero missing or incorrect characters, zero `U+FFFD` placeholders.** Also found and fixed one WeasyPrint-specific defect along the way: the mandatory lam-alef ligature ("لا") swapped its two source characters on extraction (`"الاختبار"` → `"االختبار"`), reproduced identically by both `pdftotext` and `pdfminer.six`, independent of PyMuPDF; fixed with `font-feature-settings: "liga" 0, "rlig" 0, "calt" 0, "clig" 0` in the stylesheet, which disables ligature fusion (still correctly joined, contextual Arabic letterforms — just without that one typographic flourish) and removes the bug entirely.
- The self-check (`python pdf_builder.py`) now: (a) checks PDF structural validity, (b) asserts `pdfminer.six`'s extraction contains no `U+FFFD`, (c) asserts every expected field value is present via `PyMuPDF`'s position-aware extraction (which reconstructs correct RTL reading order — confirmed to agree with real `pdftotext -layout` output; `pdfminer.six` does not reorder by position, so it is right for the completeness check and wrong for an exact-order substring check), and (d) rasterizes and confirms ink is visible across the expected content rows. Re-verified against a realistic full report (long tenant/customer names, 5 extra fields including a note that wraps across two lines).
- `pyproject.toml`: removed `fpdf2`/`uharfbuzz`, added `weasyprint` (runtime) and `pymupdf`/`pdfminer.six` (dev, self-check only). `omniflow-backend/Dockerfile` now installs WeasyPrint's system dependencies (`libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info`), committed separately since without them the import fails at container startup — not a degraded PDF, a hard failure on first use. Not yet verified inside an actual built image (only in the local Windows dev venv, where these libs are not needed and a build wasn't run this session).
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT complete:** the worker still cannot run in production, and correctly so — the actual payment gateway (item 3, Moyasar/Tap) that would publish `payment.events.v1` with a genuine `completed`/`report_fee` event does not exist yet, so there is no real trigger for this path in production. Full end-to-end verification (Kafka event → DB lookup → PDF → S3 → DB record) against live Kafka/Postgres/MinIO has not been run; that's meaningful only once item 3 exists to produce a real event. `deed_check_29` reports still only carry payment/identity metadata, not REGA-verified deed content (item 15, separate, also not done). The Docker image has not been rebuilt/tested with the new system dependencies.

### Kafka outbound pipeline — real crash/DLQ drill — 2026-09-23

Item 6 from the launch-readiness review ("validate the Kafka pending-message publisher against the real DB + Kafka and confirm retry/DLQ behavior under an actual worker crash"). New script: [`scripts/validate_kafka_outbound_recovery.py`](omniflow-backend/scripts/validate_kafka_outbound_recovery.py) (+ helper subprocess [`scripts/_crash_worker_subprocess.py`](omniflow-backend/scripts/_crash_worker_subprocess.py)).

- **What was already proven before today** (re-verified, claims checked against the actual scripts rather than taken at face value): `scripts/validate_local_transport.py` exercises real Kafka/Redis retry/DLQ/offset-commit mechanics, but only against a synthetic dummy consumer, never `OutboundDispatcherWorker` or `publish_pending_messages()`. `scripts/local_inbox_checks.py` exercises the real Postgres/RLS side of the outbound flow, but with Kafka, Redis and the WhatsApp API all mocked, and calls `process_message()` directly rather than going through `BaseKafkaConsumer.run()`/`_handle_record()` — so the retry/DLQ/offset machinery itself was never exercised together with real transport and a real DB row. Neither script involved a real process crash.
- **What's real in the new test and what's mocked:** real Postgres (`omniflow-postgres`, already-migrated local DB), real Kafka (`omniflow-redpanda`, the actual `messages.outgoing.v1` topic and its real `.dlq`), real Redis (`omniflow-redis`), the actual `publish_pending_messages()` Celery task body (not reimplemented), and the actual `OutboundDispatcherWorker.process_message()` running inside the real `BaseKafkaConsumer.run()` poll loop (not called directly). Only the WhatsApp HTTP call (`whatsapp_client.send_text_message`) is mocked, since no test WhatsApp Business number/credentials exist yet — this test is about the pipeline mechanics, not live delivery (that's item 5, separate, still open). Creates a real, committed synthetic tenant/customer/conversation (can't run inside one rolled-back transaction like the other validators, because the crash scenario needs a genuinely separate OS process to see the row) and deletes it (in dependency order — `Tenant`/`Customer`/`Conversation` FKs are `ondelete=RESTRICT` by design) in a `finally` block regardless of outcome. Refuses to run against anything but a local broker/Redis/Postgres. Ran twice back-to-back to confirm it's not a fluke, including against a topic already holding leftover records from the first run (harmless — the old orphaned messages get DLQ'd again by the new consumer group's earliest-offset replay since their synthetic tenant no longer exists, adding noise but not affecting correctness).
- **Scenario 1 — real crash mid-processing:** spawned `OutboundDispatcherWorker` as an actual OS subprocess. It received a real message from the real topic, ran the real `process_message()` (WhatsApp "send" mocked, but the Redis idempotency key and the DB `delivery_status` update are real), then — instead of returning control to `BaseKafkaConsumer` to commit the offset — blocked forever. The parent process confirmed the real work had genuinely completed (DB row already `SENT`, real Redis key set) and then called `proc.kill()` (`SIGKILL` on Linux, `TerminateProcess` on Windows — no graceful shutdown, no `finally` blocks, no chance to commit). A fresh consumer in the *same* consumer group was then started and confirmed to receive the *same* Kafka record again (proof the offset was genuinely never committed), and asserted the mocked WhatsApp send function was **not** invoked a second time. **What this proves:** a hard crash after work is done but before the Kafka commit does not produce a duplicate customer message, and the pipeline reconverges correctly. **What it does not prove:** the narrower internal race between setting the Redis idempotency key and updating the DB status (`worker.py` lines ~358–389) — my crash point was after both had already happened, so redelivery in this test short-circuited on the earlier DB-status check (`existing.delivery_status in ("SENT","DELIVERED","READ")`), not the Redis-based `already_sent` branch. That narrower window is still logically covered by the code (the `already_sent` branch itself repairs the DB status if it lags behind Redis) but was not independently exercised by a real crash today.
- **Scenario 2 — real DLQ + no head-of-line blocking:** a message whose mocked send always raises exhausted real retries (real backoff delays) and landed on the real `messages.outgoing.v1.dlq` topic with the real wrapped payload (confirmed by consuming it with an independent consumer). The DB row correctly ended up `FAILED`. A second ("canary") message published to the same real topic right after was still processed and reached `SENT` by the *same, still-running* consumer instance — proving the DLQ path doesn't stall the consumer on later records, real offset commit past the DLQ'd one included.
- **Scenario 3 — DLQ producer itself fails:** patched the real DLQ producer's `send_and_wait` (not `_publish_to_dlq` itself, to exercise the actual critical-log-then-re-raise code path in `kafka/consumer.py`) to raise, and confirmed `worker.run()` actually raises/exits rather than silently continuing past an unacknowledged record — the documented safety behavior in `kafka/consumer.py`'s module docstring was actually exercised, not just read.
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT cover:** the narrower Redis-then-DB race noted above; multi-partition/multi-replica rebalance behavior (the real topic has 1 partition); a real WhatsApp API failure mode (rate limiting, malformed webhook, etc.) — only a generic mocked exception; and the `ai_bot` sender path (`publish_pending_messages()` only sweeps `human_agent` rows — `ai_bot` replies are queued by a different, unverified-today code path per the routing worker).

### VCard real delivery + reminders — 2026-09-23

Item 7. Before this, the "gatekeeper" only flipped `Customer.vcard_state` and sent plain instructional text — no vCard file was ever built (the `vobject` dependency, added specifically for this, was never imported anywhere) and no reminder message was ever actually delivered (the reminder sweep only bumped DB state, with a `TODO(outbound)` admitting it).

- New [`vcard_builder.py`](omniflow-backend/src/ai_workers/vcard_gatekeeper/vcard_builder.py): builds a real VCard 3.0 file via `vobject` from the tenant's `business_name` and (new) `Tenant.whatsapp_display_phone_number`.
- **Product/schema decision made without waiting for sign-off** (additive, nullable, trivially reversible — not the kind of irreversible choice worth blocking on): added `Tenant.whatsapp_display_phone_number` ([migration `0010_tenant_wa_phone`](omniflow-backend/alembic/versions/0010_tenant_wa_phone.py)). The existing `Tenant.whatsapp_phone_number_id` is Meta's opaque internal ID, not a dialable number — a vCard built from it would be useless to the customer. **Needs follow-up:** nothing in onboarding captures this field yet, so every existing/new tenant's vCard will have no phone number until either onboarding is updated or it's backfilled manually. Flagging for your sign-off on where this field should be captured (onboarding flow vs. settings page).
- `WhatsAppClient` gained two real methods ([`client.py`](omniflow-backend/src/channel_adapters/whatsapp/client.py)): `upload_media()` (real multipart upload to Meta's Media API) and `send_document_message()`. Chose media upload over a public-URL-linked document specifically to avoid a dependency on item 16 (public storage), which isn't done — the vCard is generated fresh and uploaded directly, no storage bucket involved.
- `OutboundDispatcherWorker` now has a real `message_type == "vcard"` branch (previously vCard messages silently fell through to the plain-text default path and the attachment was dropped entirely).
- `vcard_tasks.advance_vcard_states()` now takes an optional `producer` and actually publishes the `reminder_1`/`reminder_2` nudge to the customer's real conversation (resolved via their most recent `Conversation` row), instead of only advancing the state machine. Backward compatible — omitting `producer` keeps the old (DB-only) behavior, which the existing `local_inbox_checks.py`/other tests rely on.
- **Verification, matching the item-6 bar** — real Postgres + real Kafka (Redpanda), through the real `VCardGatekeeperWorker`/`OutboundDispatcherWorker` consumer loops (`run()`/`_handle_record()`, not `process_message()` called directly). Only the WhatsApp network transport is faked, via `httpx.MockTransport` wired directly into `WhatsAppClient`'s real `httpx.AsyncClient` — the client's actual multipart-encoding and JSON-payload-building code runs unmodified and is what gets asserted on, not a mock of `send_text_message`/`upload_media` themselves. New script: [`scripts/validate_vcard_delivery.py`](omniflow-backend/scripts/validate_vcard_delivery.py), ran twice.
  - Scenario A: a real `NEW` customer, routed through a real `RoutingDecision` Kafka event, real gatekeeper worker advances DB state to `VCARD_SENT`; real dispatcher worker uploads a real vCard (asserted: valid `BEGIN:VCARD`, contains the tenant's actual business name and phone number) and sends a real `document`-type WhatsApp payload referencing it by media id, filename `contact.vcf`.
  - Scenario B: a customer manually backdated past the reminder threshold is swept by the real `advance_vcard_states()`, and the real reminder text is confirmed to actually reach the dispatcher and get sent — not just a DB flag flip.
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT prove:** a real phone receiving and being able to save this vCard — no WhatsApp test number/credentials exist yet (same gap as item 5). The reminder publish shares the same at-least-once risk as the human_agent path in `outbound_tasks.py` (publish before the sweep's transaction commits), but *without* a stable `message_id` for downstream dedup (each reminder gets a fresh random `source_event_id`) — an occasional duplicate reminder text is possible on a crash between publish and commit; accepted as low-severity for a nudge message, but noted honestly rather than silently carried over. Reminder de-duplication beyond the state-machine's own idempotency (a customer already moved out of `VCARD_SENT` won't be swept again) was not further hardened.

### Broadcasts/campaigns — real dispatch, delivery, and template gate — 2026-09-24

Item 8. `vip_subscribers`/`broadcast_deliveries` already existed (migration `20260813_srs_features` — the launch-readiness doc's premise that they were missing was stale). What was actually broken: `BroadcastWorker` used raw SQL against columns that don't exist at all (`broadcast_deliveries.status` instead of `delivery_status`, `vip_subscribers.list_type` which was never a real column, `customers.primary_phone` instead of `unified_phone`) and never called the WhatsApp API — `await asyncio.sleep(0.02)` with a comment claiming to "simulate" the send. It would have crashed on its first real recipient. There was also no Kafka-publish wiring from `schedule_campaign` (the router's own docstring admitted this) and no Meta template-approval gate anywhere.

- **Product decision made without waiting for sign-off** (per the "pick the simplest default matching the SRS and flag it" instruction): audience segmentation reuses the *existing* `all | vip | engaged | new | inactive` segments already implemented in `gateway/routers/broadcasts.py`'s `preview-audience` endpoint (VIP = `Customer.is_vip`, no separate named "lists"/tiers). This sidesteps the `vip_subscribers.list_type` scheme the old dead code assumed but never actually had a column for. **Needs your sign-off:** if the product actually wants multiple named VIP lists/tiers (not just a flat VIP flag plus engagement-based segments), that's a real, separate feature — the current `vip_subscribers` table (status/opted_in_at/opted_out_at/messages_sent_this_week) supports single-list opt-in tracking, not multi-tier membership.
- **The explicit ask — a pre-send Meta template-approval gate ("so free-text broadcast sends can't risk a number ban")** — implemented at two layers: `POST /{id}/schedule` now refuses (422 `TEMPLATE_REQUIRED`) to schedule a campaign without `meta_template_id`; the new `omniflow.broadcast_dispatch_check` Beat sweep (every minute) re-checks the same thing immediately before publishing to Kafka, since Meta can revoke template approval after scheduling. Sends now go through `WhatsAppClient.send_template_message` (already existed, was never called by anything) instead of free text.
- **Scheduling**: new Beat task `omniflow.broadcast_dispatch_check` finds `SCHEDULED` campaigns whose `scheduled_at` has arrived, publishes a real `BroadcastEvent` ([`shared/events/broadcast.py`](omniflow-backend/src/shared/events/broadcast.py), new) to `broadcast.marketing.v1`, and flips status to `SENDING` — closing the gap the router admitted ("wiring scheduled_at to an actual Kafka publish is a separate piece of work").
- **Delivery worker**: [`broadcast_worker/worker.py`](omniflow-backend/src/ai_workers/broadcast_worker/worker.py) rewritten from scratch on the ORM (no more raw-SQL column-name bugs), resolves the real audience per segment, sends a real template message per recipient (mocked network transport only, see below), rate-limited at 50/sec, records a real `BroadcastDelivery` row per recipient (`delivery_status`, real `platform_message_id`/wamid, `error_detail` on failure), and is idempotent against Kafka redelivery via the table's own `(campaign_id, customer_id)` unique constraint. Reuses `outbound_dispatcher`'s `_resolve_tenant_credentials` rather than duplicating tenant-credential resolution.
- **Delivery tracking**: only `SENT`/`FAILED` at send time is covered. `DELIVERED`/`READ` status updates depend on Meta's outbound status-callback webhook, which **does not exist anywhere in this codebase for any message type**, not just broadcasts — checked `gateway/routers/webhooks.py`, which only handles Clerk auth events. This is bigger than item 8 (it affects every WhatsApp send in the product, including item 6's human/AI messages) and wasn't built here; `broadcast_deliveries.delivered_at`/`READ` status will stay unset until that webhook exists.
- **Verification** — same bar as items 6/7: real Postgres + real Kafka, through the actual `_broadcast_dispatch_check_async()` task body and the real `BroadcastWorker.run()`/`_handle_record()` loop, only the WhatsApp network transport faked via `httpx.MockTransport`. New script: [`scripts/validate_broadcast_delivery.py`](omniflow-backend/scripts/validate_broadcast_delivery.py), ran twice. Scenario A: a template-less campaign is rejected by the real sweep before ever reaching Kafka. Scenario B: a VIP-segment campaign is dispatched, consumed, and sent to exactly the VIP-flagged customer (not the non-VIP one also in the DB), with a correctly-recorded delivery row. Full backend suite (37 tests) still passes.
- **What this does NOT cover** (real SRS §5.6 gaps, not silently claimed as done): the 3-messages/week frequency cap (`messages_sent_this_week` column exists, nothing increments or enforces it); opt-out keyword handling (no inbound handler flips `vip_subscribers.status`); the 90-day re-ask cooldown on decline; per-recipient template parameter substitution (`send_template_message` is called with no `components`, so a template with `{{1}}`-style placeholders would send them literally — fine for a static-text template, not for a personalized one); and, as above, `DELIVERED`/`READ` tracking.

### Real RAG embeddings + verified tenant isolation — 2026-09-24

Item 9. Retrieval was disabled outright (`rag_unavailable_no_embedding_provider`) whenever no OpenAI key was configured. The alternative was never "no RAG" in the code, though — `embedder.py` had a `random.seed(text)`-based mock-vector path that was semantically meaningless, and the team correctly chose to disable retrieval rather than run on it. What was actually missing: `fastembed`/`sentence-transformers`/`qdrant-client[fastembed]` were already project dependencies for exactly this purpose (real, local, no-API-key embeddings) and were never imported or wired into any code path — not a fundamental blocker, just unfinished wiring, plus `fastembed` wasn't even installed in the local dev venv despite being declared.

- `embedder.py` rewritten to support two real providers: OpenAI (`text-embedding-3-small`, 1536-dim) when a real key is configured, otherwise local `fastembed`/`BAAI/bge-small-en-v1.5` (384-dim, ONNX, CPU, no API key or ongoing network dependency — only a one-time model download cached to disk). The random-vector mock is gone entirely; there is no more "no embedding provider" state.
- `shared/core/config.py` gained `embedding_provider`/`embedding_dim` computed properties as the single source of truth for which provider is active and its vector size — `shared/qdrant_client/client.py`'s `EMBEDDING_DIM` (previously its own separately-hardcoded `1536` constant, a second latent source of drift) now reads the same value, so the two can never disagree again.
- `llm_invoker/worker.py`'s `_rag_enabled` gate removed — RAG is now always enabled, since a real embedding path always exists.
- **Verified with a real, running Qdrant** (docker-compose service) and the real local fastembed model — no mocks. New script: [`scripts/validate_rag_embeddings.py`](omniflow-backend/scripts/validate_rag_embeddings.py), ran three times (real model download/cache confirmed working; one run left two Qdrant collections uncleaned after an unrelated assertion failure during script iteration — deleted manually and confirmed the underlying `delete_collection` call works correctly when invoked directly, so this looks like a one-off timing issue during my own iteration, not a reproducible bug in the cleanup path, but flagging it rather than asserting certainty).
  - Real tenant isolation, empirically: two tenants' listings ingested into their own real per-tenant Qdrant collections; a real estate query against each tenant returns *only* that tenant's listing, never the other's.
  - Real semantic signal, not just structural isolation: the same query scores measurably higher against a relevant listing than against a semantically unrelated one (0.853 vs 0.832 cosine similarity) — the vectors carry genuine meaning, not noise.
  - The real production code path — `RAGRetriever.get_rag_context()`, the actual function the L2 routing tier calls — returns a real, correctly-formatted context string built from the real retrieved listing.
- Full backend suite (37 tests) still passes.
- **What this does NOT cover:** the Instagram-path isolation concern the item specifically named was investigated by inspection (Instagram's `_resolve_tenant_id(page_id)` feeds the same canonical event → `RoutingDecision` → RAG code path every other channel uses, no separate/leaky code path exists) but not exercised by a real Instagram message end-to-end — no Instagram test credentials exist yet. Retrieval quality with the smaller local 384-dim model vs. OpenAI's 1536-dim embeddings was not benchmarked — fastembed is real and semantically meaningful, not necessarily equally accurate; a tenant with an OpenAI key still gets the higher-quality provider automatically. Production Docker images need to either bake in the fastembed model files or accept a one-time ~30s download on first startup — not yet reflected in the Dockerfile.

### Real-estate recommendation panel — 2026-09-24

Item 10. The inbox context panel's recommendations array was a hardcoded empty list with no backend behind it at all. While wiring it up, found and fixed a more consequential bug in the same area: `shared/services/vector_sync.py` — the function the *properties API actually calls* whenever a real listing is created or updated — had its own independent, undocumented-outside-a-comment embedding implementation that fell back to a **constant** dummy vector (`[0.1] * 1536`, identical for every listing) whenever no OpenAI key was configured. That's worse than item 9's random-vector mock: every real listing would have looked equally "relevant" to any query, silently poisoning both AI-facing RAG and this new recommendation panel. Fixed by deleting that duplicate implementation and routing it through the same `EmbeddingService` singleton item 9 already fixed — one real embedding implementation now, not three independent ones that could each drift.

- **Product/design decision made without waiting for sign-off** (per the "pick the simplest default matching what exists, flag it" instruction — there's no SRS spec for this feature at all, it doesn't appear in either SRS document): new `GET /api/v1/conversations/{id}/recommendations` endpoint builds its Qdrant query from the conversation's own last 5 **customer** messages (concatenated), with no separate LLM/rule-based preference-extraction step. `score` is Qdrant's raw cosine similarity, not a bespoke weighted match-rule score. **Needs your sign-off:** if the product wants an explicit "extracted budget/area/bedrooms" step (the `intent`/`budget`/`lookingIn`/`propertyType` fields already declared in the frontend's `Conversation` type but never populated by anything — a related, pre-existing, separate gap this item does not fix), that's real additional work, not a config toggle on top of this.
- Frontend: `CustomerContext.tsx`'s hardcoded `[]` replaced with a real fetch (`lib/api/inbox.ts::fetchRecommendations`) on conversation change; empty-state text still shows correctly when a conversation has no customer messages yet or nothing scores above zero.
- **Verified with real Postgres + real Qdrant + real fastembed**, through the actual `vector_sync.sync_listing_to_qdrant` ingestion function (the same one the properties API calls) and the actual `get_conversation_recommendations` endpoint function — real dependencies constructed manually (real DB session, real repository), not the full FastAPI/HTTP/Clerk-auth layer, which is an acknowledged scope reduction, stated plainly rather than implied as full coverage. New script: [`scripts/validate_recommendations.py`](omniflow-backend/scripts/validate_recommendations.py), ran twice — a real listing ("apartment, Narjis, 1.15M SAR, 210m²") is ingested via the real production embedding path and correctly retrieved and formatted (score 0.858) by the real endpoint against a conversation whose customer wrote "أبحث عن شقة 3 غرف في حي النرجس بالرياض". Frontend: `tsc --noEmit`, `eslint` (0 warnings on the changed files), full Next.js production build, and the existing 12-test frontend suite all pass. Backend suite (37 tests) still passes.
- **What this does NOT cover:** the extracted-preferences fields (`budget`/`lookingIn`/`propertyType`/`intent`) shown elsewhere in the same panel remain unpopulated — a separate, pre-existing gap, not part of this item; no A/B or quality evaluation of "query = recent customer messages" vs. a proper extraction step; the endpoint was not exercised through a real HTTP request with real Clerk auth end-to-end.

## Next steps and known gaps

1. Real database inbox/report integration and synthetic Kafka/Redis transport checks pass. Next validate the authenticated browser journey and the combined worker flow; the inbox validator still mocks transport/channel boundaries.
2. Validate the pending-message publisher with the real database and Kafka. Review recovery of QUEUED/FAILED messages and DLQ operations. Delivery cannot promise exactly-once across a crash between external API acceptance and durable acknowledgement.
3. SSE reconnect/cancellation and list/history refresh are implemented and unit-tested. Still validate them in an authenticated browser, complete message-history pagination for long conversations, and implement delivery-status UI/SSE updates.
4. Shared chat provider wiring is complete and live-tested with Groq. Complete real embedding/retrieval support and verify tenant isolation in the Instagram path.
5. Complete real VCard contact sending and missing production worker wiring. Current VCard state changes do not prove a contact was delivered.
6. Replace report/PDF, broadcast, payment, and other placeholder paths with the behavior required by the SRS. Implement and verify the remaining channels and product journeys.
7. Produce immutable release images and validate the complete staging stack, off-host backups, Qdrant/MinIO restore, public storage and observability. Both local Docker verification images contain the final source; the PostgreSQL restore drill passed and development infrastructure checks are healthy. This does not establish complete production-stack readiness.
8. Audit remaining concurrency risks: conversation counters, takeover ownership races, user provisioning races, and SSE publication before request-transaction commit.

The product is not complete. Passing unit tests and a frontend build must not be presented as end-to-end or production verification.
