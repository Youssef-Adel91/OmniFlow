# Implementation status — 2026-09-23

This is an engineering handoff, not a production-readiness declaration. The SRS remains the product specification. Preserve the existing architecture, naming, comments, and writing style when extending this work.

## Working tree

All work is uncommitted. Before this work, the user already had changes in `omniflow-backend/src/shared/core/config.py` (OpenAI-compatible provider settings) and `src/ai_engine/llm_orchestrator.py` (whitespace). Preserve those changes. No production deployment or real customer messages were performed.

## Implemented in this working tree

- Fixed the configuration syntax error and retained legacy model settings needed by existing workers.
- Bound tenant-facing repository sessions and SSE authentication to the authenticated user. Removed header-selected tenant context and rejected the reserved system tenant ID.
- Bound Clerk JWT verification to a configured/trusted issuer before fetching JWKS, with required claims and authorized-party checks.
- Added migration `0009_message_rls` to protect direct message queries through their owning conversation. The migration chain and actual RLS behavior passed under a non-superuser/non-BYPASSRLS role. The local application database was subsequently upgraded after a backup (see production hardening below).
- Corrected human takeover assignment, ownership checks, status normalization, and REST/SSE message mapping. Human sending currently supports WhatsApp only.
- Added a database-backed pending-message publisher scheduled by Celery Beat. Stable message IDs are shared with the dispatcher. Deployment requires Beat, a Celery worker, Kafka, and the outbound dispatcher.
- Changed outbound deduplication to record successful sends, permit retries after failures, and record Meta API acceptance as SENT rather than DELIVERED.
- Kafka commits only the processed record offset. A failed DLQ publish now terminates the consumer instead of allowing a later batch to acknowledge past the failed record.
- Inbound duplicate handling uses a savepoint and verifies the existing message before treating an integrity error as a duplicate; it no longer rolls back the entire transaction.
- Routing reads authoritative conversation/privacy state from the database. L0 replies reach the outgoing queue. LLM timeout escalation updates the conversation instead of publishing an incompatible event to the outgoing queue, preserving concurrent takeover/closure.
- Added backend/frontend Dockerfiles and ignore files, frontend standalone output, and build arguments in production Compose. Both images now build; container checks are recorded below. The complete production stack has not been deployed.
- Replaced the obsolete `/test` scratch page with its existing redirect behavior in Next configuration, fixing the production build's missing-root-layout failure.

## Verification

### Production hardening and container verification — 2026-09-23

- `/health` remains process liveness. `/health/ready` probes PostgreSQL, Redis and Kafka with bounded timeouts, returns 503 on dependency failure, and omits exception details. Production startup now requires these services and rejects a database role with superuser/BYPASSRLS privileges. The local endpoint returned all three checks `ok`.
- Production validation uses explicit exceptions, including under `python -O`, for debug mode, missing authentication, mock secrets/keys, unsafe CORS, and unconfigured voice output. Mock audio/storage and unfinished paid-report generation fail closed in production. Development report placeholders are no longer marked delivered. These guards do not complete the missing integrations.
- Production Compose separates migration-owner credentials from `APP_POSTGRES_USER` / `APP_POSTGRES_PASSWORD`. Deployment builds the maintenance profile, migrates before starting compute services, and provisions a nonprivileged application role. `provision-app-role.sql` was exercised twice in a disposable PostgreSQL container: grants, default grants, tenant RLS, and rejection of the admin role passed. No production roles were changed.
- Redis uses `noeviction`; MinIO initialization creates the configured private buckets; gateway access logging is disabled and Nginx access logs omit query strings. Global Docker image pruning was removed from deployment. Query-bearing error logs and complete storage ingress still need review.
- Updated Next.js to 16.3.6, React to 19, next-intl to 4, and affected dependencies. Converted route params to the asynchronous contract, migrated authentication to `proxy.ts`, and retained existing UI/layout conventions. Removed the random, non-persisted onboarding request number. `npm audit fix` and the Linux image's `npm ci` reported **0 vulnerabilities** at verification time.
- Frontend Docker build/runtime now use supported Node.js 24 LTS (verified runtime: 24.21.0), replacing Node.js 20 after its end of life. The final Linux build and TypeScript check passed on Node 24 and `npm ci` again reported zero known vulnerabilities. Support schedule: https://nodejs.org/en/about/previous-releases.
- Frontend: **12 tests passed**, production build/TypeScript passed, ESLint has **0 errors and 104 warnings**. The newly introduced `set-state-in-effect` performance rule is warning-level while existing loading effects remain; this is not a warning-free codebase. Clerk React 5 and ESLint 9 also emit upstream deprecation notices.
- Backend: **37 tests passed locally and inside the Linux image**, with no network access or real credentials in the container tests. Production guard tests also passed with Python optimization. `pip check` and imports of all three deployed Kafka workers plus Celery passed in the image. Docker verification tags are local, not published release tags.
- Backend image currently includes the complete ML/document-processing dependency set and is approximately **6.89 GB**; it installs CPU PyTorch instead of CUDA. Dependencies are still not locked. The final verification image reuses the successfully built dependency layer with the final application/Alembic source copied over it; `pip check`, all 37 tests, and offline migrations with reserved password characters passed again. A redundant clean dependency download was cancelled, not counted as a successful clean rebuild. Produce immutable release images from the final reviewed commit before deployment.
- The existing local database was at `9e62a0b74221`, missing the settings columns. Created a custom-format dump at ignored `omniflow-deploy/backups/postgres/local-before-readiness-20260923.dump`, then applied through `0009_message_rls`. The backup was subsequently restored successfully into a disposable, network-isolated PostgreSQL container, verifying the saved revision and row counts; that container and its volume were removed. Authenticated settings now load. Alembic escapes URL percent signs and disables asyncpg statement caches for PgBouncer.
- Re-ran the isolated full migration/RLS/inbox/report validator after the upgrade: passed, with its temporary transaction rolled back. Channel sends remain mocked in this validator.
- Fixed false-negative development health checks: PgBouncer now queries the application database instead of its restricted admin console; Qdrant no longer calls missing curl; Redpanda no longer depends on output spacing. All three local containers now report healthy, retaining their volumes.
- Backup now fails on missing Qdrant/MinIO or failed Qdrant listing and skips all retention pruning when any required backup failed. A network-isolated fake-Docker test verifies both failure cases and per-bucket destination separation. Default MinIO backup coverage now includes media, reports, temporary media, public assets and knowledge documents; `MINIO_SOURCE_BUCKETS` can override the list.
- Added the private knowledge bucket to production initialization. Executed the actual initialization commands against a disposable MinIO instance: all buckets were created, report/knowledge access remained private, and report versioning was enabled. Removed the temporary instance afterwards. This does not test public HTTPS storage ingress or a full object-store restore.
- Authenticated browser verification on the upgraded standalone frontend passed for inbox and settings. After a local restart, the frontend Docker image was started as `omniflow-frontend-validation` on **127.0.0.1:3000**; the existing Clerk session and synthetic inbox loaded from that image. Backend remains the local Python process on port 8000 with debug/access logging disabled. This mixed local setup is not the production stack.
- Removed hard-coded property recommendations/prices/match scores from the inbox context panel. It now shows an empty state until real conversation recommendations are wired in. The three previously inert quick-action buttons are disabled with an availability message; this does not implement those actions.

**Remaining launch gates:** real channel/worker end-to-end delivery, real embeddings and retrieval, payment/report generation, public storage URLs and bucket coverage, actual observability wiring (Sentry/OTel settings alone are not instrumentation), backup restore/off-host retention, dependency locking/scanning for Python, production domain/TLS/provider credentials, and staging acceptance/load tests. No public deployment or real customer communication occurred.

### Authenticated browser verification — 2026-09-23

- User signed in through Clerk. Initial 401s came from the sandboxed gateway failing to fetch the configured Clerk public signing keys. Restarting the local gateway with approved network access restored signature verification; no authentication bypass was added.
- Dashboard pages now wait for Clerk initialization before mounting data-fetching effects. A fresh browser reload showed the loading state followed by the authenticated inbox without the previous transient 401 banner.
- Fixed the Sidebar overview link from `/` (marketing page) to `/dashboard`; clicking it now shows real dashboard counts.
- Fixed legacy naive-UTC conversation timestamps in the API response. A newly created conversation previously displayed as three hours old; the browser now displays the correct elapsed minutes. Both naive UTC and offset-aware timestamps are regression-tested.
- Created one explicitly labelled local synthetic Web conversation (`local-browser-validation`, customer `عميل اختبار محلي`) with two test messages. It remains available for manual testing. No real channel send or customer communication was performed.
- Verified in the signed-in browser: list loading, message history, human takeover, return to AI, status persistence after reload, report empty state/revenue buckets, and dashboard counts. A second synthetic inbound message went through actual PostgreSQL persistence and Redis publication and appeared in the open conversation without a page reload.
- SQL echo now follows `APP_DEBUG` rather than automatically logging bound values in every development session. Current local gateway starts with debug/access logging disabled; replacing JWT query parameters in SSE with a stronger transport strategy remains follow-up work.
- Fresh verification: **27 backend tests**, **12 frontend tests**, TypeScript and Next production build passed. Existing lint warnings remain. Paid-report generation, payments, real channel delivery, and full-product acceptance are still outstanding.

### Reports and transport update — 2026-09-23

- Report detail and vault replies now obtain 15-minute signed download URLs. Signing validates the configured storage host/bucket and the owning tenant/customer object path. The UI requests a fresh URL on every download and does not fall back to an expired permanent URL.
- Report list exposes file availability as `ready` / `pending` without exposing permanent storage URLs. Frontend reads `price_sar`, revenue `series`, and the SRS report-type labels. Inclusive UTC date filters are implemented and reversed ranges return 422.
- `scripts/local_report_checks.py`, called by the isolated inbox validator, passed against PostgreSQL: both date boundaries, pagination, prices/status, daily revenue, cross-tenant 404, and no signing of inaccessible reports. All synthetic rows/schema/role were rolled back.
- `scripts/validate_local_transport.py` passed real Kafka/Redis publish, transient retry, successful replay deduplication, DLQ routing and offset commits. It creates/deletes only its own temporary topics and removes its completion keys from the cache Redis database. Producer idempotence is enabled for broker retries within a producer session.
- `scripts/validate_local_storage.py` previously passed a real MinIO round trip with synthetic text: signed GET succeeds, unsigned access is denied, and its temporary object is deleted. No report content or PDF generation is validated by that check.
- Current checks: **26 backend unit tests**, **12 frontend tests**, TypeScript, and the real PostgreSQL/transport checks passed. Backend unit discovery must use `.venv\\Scripts\\python.exe -m unittest discover -s tests -t . -q` so `tests/__init__.py` isolates live settings. Frontend tests can run with `node --test --test-isolation=none tests/*.test.cjs` when the sandbox disallows child processes.
- The local browser renders Clerk sign-in. Authenticated UI testing remains pending a user-provided test session; no authentication bypass was added.
- The updated Next production build passed (existing lint warnings remain). Gateway was restarted and checked for health, the new report parameters/status in OpenAPI, and rejection of unauthenticated report access.
- **Unfinished report generation:** the development placeholder is still plain UTF-8 text with a `.pdf` extension. Production now rejects it and it is no longer marked delivered. Payment integration and source-backed report content are not complete. Download/DTO fixes must not be described as completion of paid reports.

### Inbox reliability update — 2026-09-22

- SSE reconnect obtains a current authentication token, cancels obsolete asynchronous connection attempts, and uses bounded retry delays. Opening the stream refreshes the conversation list and active message cache.
- Unknown-conversation events trigger a list refresh. Repeated SSE messages do not increment the global unread badge twice; outgoing messages do not increment conversation unread counts.
- Optimistic replies use unique temporary IDs. REST confirmation removes an already-arrived SSE duplicate; failure removes only the corresponding pending reply. Forced history refresh merges concurrent SSE arrivals rather than overwriting them.
- Fresh verification: **9 frontend tests**, **23 backend tests**, TypeScript, and a full Next production build passed. Existing lint warnings remain. Browser/authenticated user-journey testing remains outstanding.
- The real PostgreSQL validator previously passed two-tenant RLS SELECT/UPDATE isolation using a non-superuser/non-BYPASSRLS role, same-phone separation, duplicate inbound persistence, takeover, outbox rollback/retry, and return to AI. Everything ran inside a temporary schema and transaction that was rolled back.
- After Docker became available, `.venv\Scripts\python.exe scripts/validate_local_migrations.py --via-pgbouncer --inbox` passed the full migration chain and the real database checks, including channel-send failure/retry, durable SENT state, completed-send replay suppression, cancelling an AI reply after takeover, and return to AI. Transport calls use mocks; this is not a live WhatsApp send. The temporary schema, role, and all synthetic data were rolled back.

### Groq integration added after the initial audit

- User supplied a Groq key. It is stored only in ignored `omniflow-backend/.env`; never copy it into this document, examples, logs, or Git.
- Shared provider configuration now connects both the gateway orchestrator and the existing invocation worker to the selected OpenAI-compatible service. Existing worker class/response contracts are retained.
- Local provider is `groq`, endpoint `https://api.groq.com/openai/v1`. ROUTER/L1 use `openai/gpt-oss-20b`; L2/L3 use `openai/gpt-oss-120b`. No fallback to another provider is configured.
- Both models are listed in Groq's free-plan limits at https://console.groq.com/docs/rate-limits. Account billing tier cannot be inferred from an API key; free usage depends on the account plan and quotas. `LLM_FREE_ONLY` specifically validates OpenRouter `:free` routes and is false for Groq; it is not a Groq billing control.
- Live synthetic requests to both models passed. `scripts/smoke_llm.py --application` also passed the gateway classifier, gateway response, and worker response using supplied synthetic property context. This did not send channel messages or use customer data.
- Backend suite now passes **23 tests**, including provider key isolation, model overrides, OpenRouter free-route validation, reasoning output handling, and worker history/context/token metadata.
- Worker startup skips vector retrieval when no real embedding key exists, logging `rag_unavailable_no_embedding_provider`. This avoids treating the legacy random-vector mock as genuine RAG. Live document retrieval, speech, and image processing are not covered by these chat tests.
- Restart already-running gateway/worker processes to load the new environment. No background workers were started by the smoke tests.

- Backend: ` .venv\Scripts\python.exe -m unittest tests.test_delivery tests.test_security -q` — **18 tests passed**. These exercise real application functions with mocked external boundaries; they do not establish live database RLS or full delivery integration.
- Frontend: `node --test tests/inbox-contract.test.cjs` — **3 tests passed**.
- Frontend: TypeScript check and full Next production build passed. ESLint warnings remain.
- `git diff --check` passed, with Git line-ending conversion warnings.
- Local isolated migration validator: `.venv\Scripts\python.exe scripts/validate_local_migrations.py --via-pgbouncer` — **full chain passed through `0009_message_rls`**, and the temporary transaction was rolled back. The script refuses remote database hosts. Alembic accepts a supplied connection for this validation.
- Database diagnosis: project credentials match Docker configuration, and a real query through PgBouncer on 6432 succeeds. Windows has a native `postgres` process listening on IPv4 port 5432 alongside Docker's listener. Direct host connections fail authentication; do not conclude the Docker database is down or change its password. No services were stopped and no database credentials were changed.

### Secret-exposure and legacy-auth check — 2026-09-23

- Searched full `git log --all -p` (this repo has a single squashed commit, "Initial commit for OmniFlow") for real Gemini/Meta credential values and for the literal token prefixes present in the local untracked `.env` (`EAAYni...`, `AQ.Ab8...`). No matches: every `GEMINI_API_KEY=`/`*_ACCESS_TOKEN=` line ever committed is a `<REQUIRED>` placeholder, and `.env`/`.env.local` were never tracked. **This means the exposed-secret concern from the prior audit does not apply to this repo's Git history as it exists now** — there is nothing to purge with `git filter-repo`/BFG, and no force-push is needed. This does not confirm whether the real keys were exposed elsewhere (a different prior repo state, chat, CI logs) before this history was squashed.
- The real Gemini and Meta Instagram credential values still sit in the untracked local `omniflow-backend/.env`. Per the user's own account these were exposed previously — **rotating them at the Google AI Studio / Meta developer consoles is a provider-side action only the account owner can perform; still needs to be done regardless of the clean git history.**
- Added `.pre-commit-config.yaml` (gitleaks hook) at the repo root to catch secrets before they're committed going forward. Not yet activated — requires `pip install pre-commit && pre-commit install` once, which needs to be run by whoever has write access to this checkout (not run by this session).
- Confirmed item 2 (legacy local-JWT paths) is already fully done in this working tree: `POST /login`, `/register`, `/refresh` in [auth.py](omniflow-backend/src/gateway/routers/auth.py) return HTTP 410 Gone and mint nothing; `PATCH /tenants/onboarding` in [tenants.py](omniflow-backend/src/gateway/routers/tenants.py) never issues a token (only updates `onboarding_status`); `DEMO_CREDENTIALS` does not exist anywhere in the frontend; the `TestBotSimulator`/`localhost:8000` widget is fully removed from [inbox/page.tsx](omniflow-frontend/src/app/[locale]/(dashboard)/inbox/page.tsx), leaving only an explanatory comment. No code changes were needed for item 2.

### Real PDF report generation — 2026-09-23

- Replaced the placeholder in [`vault_worker/generator.py`](omniflow-backend/src/ai_workers/vault_worker/generator.py) (was `f"OmniFlow AI Report: ...".encode("utf-8")` saved with a `.pdf` extension) with a genuine PDF built by the new [`pdf_builder.py`](omniflow-backend/src/ai_workers/vault_worker/pdf_builder.py) module, populated with real data pulled from the database at generation time: the tenant's `business_name`, the customer's `display_name`/`unified_phone` (queried live via `get_tenant_session`), the price paid, the transaction reference, the report type, and the generation timestamp. Any additional metadata keys on the payment event (e.g. a future deed number) render as extra report fields without code changes.
- **This went through three real, user-caught rounds before landing — recorded in full because each round found a genuine defect the previous self-check missed, not a flaky retest:**
  1. **First version (fpdf2 + manual `arabic_reshaper`/`python-bidi`):** self-check only verified PDF structural validity (header/trailer/size), never what was actually on the page. User caught it visually: Arabic letters rendered disconnected, and every row after the tenant name was missing from the page entirely (present in the raw byte stream, off-canvas — `multi_cell`'s default `new_x=XPos.RIGHT` leaves the cursor at the right edge of the previous line instead of resetting to the left margin).
  2. **Second version (fpdf2 + real HarfBuzz shaping via `set_text_shaping()`, cursor bug fixed):** fixed both visual bugs — verified by rasterizing and eyeballing a full realistic report. User then asked for the specific, correct test: does the PDF's actual **text layer** (not the picture of it) survive `pdftotext`/select-all? It did not. Investigation (downloaded real poppler `pdftotext`, installed `pikepdf`/`pdfminer.six`/`uharfbuzz` for direct inspection) found a real, reproducible fpdf2 bug: `TTFFont.shape_text` builds a HarfBuzz-cluster-to-source-character map and `.pop()`s each cluster the first time a glyph consumes it, so any glyph sharing a cluster with another (routine for Arabic — e.g. a base+mark pair) gets silently dropped from the PDF's `/ToUnicode` CMap. Confirmed against fpdf2 2.8.8 **and** an unreleased git-master build (commit `4287c924`, which does fix a related-but-different bug — [py-pdf/fpdf2#1952](https://github.com/py-pdf/fpdf2/issues/1952), the 100-entries-per-`bfchar`-block limit — but not this one). Effect: ~15-20% of glyphs on a page this size render perfectly on screen but are unextractable — garbled or missing on copy/paste, search, or a screen reader. A post-processing patch (`pikepdf`-based, filling the gaps with an explicit `U+FFFD` per missing code) reduced but did not eliminate the problem.
  3. **Final version:** rather than keep patching fpdf2/HarfBuzz internals, switched the whole PDF backend to **WeasyPrint** (HTML/CSS → PDF via Pango). Independently re-verified with real `pdftotext` (poppler, downloaded and run directly — not a Python wrapper) and `pdfminer.six` (a spec-literal ToUnicode-CMap extractor, unlike PyMuPDF which appears to reverse-engineer text from the embedded font directly and therefore doesn't actually exercise the `/ToUnicode` CMap this check cares about): **zero missing or incorrect characters, zero `U+FFFD` placeholders.** Also found and fixed one WeasyPrint-specific defect along the way: the mandatory lam-alef ligature ("لا") swapped its two source characters on extraction (`"الاختبار"` → `"االختبار"`), reproduced identically by both `pdftotext` and `pdfminer.six`, independent of PyMuPDF; fixed with `font-feature-settings: "liga" 0, "rlig" 0, "calt" 0, "clig" 0` in the stylesheet, which disables ligature fusion (still correctly joined, contextual Arabic letterforms — just without that one typographic flourish) and removes the bug entirely.
- The self-check (`python pdf_builder.py`) now: (a) checks PDF structural validity, (b) asserts `pdfminer.six`'s extraction contains no `U+FFFD`, (c) asserts every expected field value is present via `PyMuPDF`'s position-aware extraction (which reconstructs correct RTL reading order — confirmed to agree with real `pdftotext -layout` output; `pdfminer.six` does not reorder by position, so it is right for the completeness check and wrong for an exact-order substring check), and (d) rasterizes and confirms ink is visible across the expected content rows. Re-verified against a realistic full report (long tenant/customer names, 5 extra fields including a note that wraps across two lines).
- `pyproject.toml`: removed `fpdf2`/`uharfbuzz`, added `weasyprint` (runtime) and `pymupdf`/`pdfminer.six` (dev, self-check only). `omniflow-backend/Dockerfile` now installs WeasyPrint's system dependencies (`libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info`), committed separately since without them the import fails at container startup — not a degraded PDF, a hard failure on first use. Not yet verified inside an actual built image (only in the local Windows dev venv, where these libs are not needed and a build wasn't run this session).
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT complete:** the worker still cannot run in production, and correctly so — the actual payment gateway (item 3, Moyasar/Tap) that would publish `payment.events.v1` with a genuine `completed`/`report_fee` event does not exist yet, so there is no real trigger for this path in production. Full end-to-end verification (Kafka event → DB lookup → PDF → S3 → DB record) against live Kafka/Postgres/MinIO has not been run; that's meaningful only once item 3 exists to produce a real event. `deed_check_29` reports still only carry payment/identity metadata, not REGA-verified deed content (item 15, separate, also not done). The Docker image has not been rebuilt/tested with the new system dependencies.

### Kafka outbound pipeline — real crash/DLQ drill — 2026-09-23

Item 6 from the launch-readiness review ("validate the Kafka pending-message publisher against the real DB + Kafka and confirm retry/DLQ behavior under an actual worker crash"). New script: [`scripts/validate_kafka_outbound_recovery.py`](omniflow-backend/scripts/validate_kafka_outbound_recovery.py) (+ helper subprocess [`scripts/_crash_worker_subprocess.py`](omniflow-backend/scripts/_crash_worker_subprocess.py)).

- **What was already proven before today** (re-verified, claims checked against the actual scripts rather than taken at face value): `scripts/validate_local_transport.py` exercises real Kafka/Redis retry/DLQ/offset-commit mechanics, but only against a synthetic dummy consumer, never `OutboundDispatcherWorker` or `publish_pending_messages()`. `scripts/local_inbox_checks.py` exercises the real Postgres/RLS side of the outbound flow, but with Kafka, Redis and the WhatsApp API all mocked, and calls `process_message()` directly rather than going through `BaseKafkaConsumer.run()`/`_handle_record()` — so the retry/DLQ/offset machinery itself was never exercised together with real transport and a real DB row. Neither script involved a real process crash.
- **What's real in the new test and what's mocked:** real Postgres (`omniflow-postgres`, already-migrated local DB), real Kafka (`omniflow-redpanda`, the actual `messages.outgoing.v1` topic and its real `.dlq`), real Redis (`omniflow-redis`), the actual `publish_pending_messages()` Celery task body (not reimplemented), and the actual `OutboundDispatcherWorker.process_message()` running inside the real `BaseKafkaConsumer.run()` poll loop (not called directly). Only the WhatsApp HTTP call (`whatsapp_client.send_text_message`) is mocked, since no test WhatsApp Business number/credentials exist yet — this test is about the pipeline mechanics, not live delivery (that's item 5, separate, still open). Creates a real, committed synthetic tenant/customer/conversation (can't run inside one rolled-back transaction like the other validators, because the crash scenario needs a genuinely separate OS process to see the row) and deletes it (in dependency order — `Tenant`/`Customer`/`Conversation` FKs are `ondelete=RESTRICT` by design) in a `finally` block regardless of outcome. Refuses to run against anything but a local broker/Redis/Postgres. Ran twice back-to-back to confirm it's not a fluke, including against a topic already holding leftover records from the first run (harmless — the old orphaned messages get DLQ'd again by the new consumer group's earliest-offset replay since their synthetic tenant no longer exists, adding noise but not affecting correctness).
- **Scenario 1 — real crash mid-processing:** spawned `OutboundDispatcherWorker` as an actual OS subprocess. It received a real message from the real topic, ran the real `process_message()` (WhatsApp "send" mocked, but the Redis idempotency key and the DB `delivery_status` update are real), then — instead of returning control to `BaseKafkaConsumer` to commit the offset — blocked forever. The parent process confirmed the real work had genuinely completed (DB row already `SENT`, real Redis key set) and then called `proc.kill()` (`SIGKILL` on Linux, `TerminateProcess` on Windows — no graceful shutdown, no `finally` blocks, no chance to commit). A fresh consumer in the *same* consumer group was then started and confirmed to receive the *same* Kafka record again (proof the offset was genuinely never committed), and asserted the mocked WhatsApp send function was **not** invoked a second time. **What this proves:** a hard crash after work is done but before the Kafka commit does not produce a duplicate customer message, and the pipeline reconverges correctly. **What it does not prove:** the narrower internal race between setting the Redis idempotency key and updating the DB status (`worker.py` lines ~358–389) — my crash point was after both had already happened, so redelivery in this test short-circuited on the earlier DB-status check (`existing.delivery_status in ("SENT","DELIVERED","READ")`), not the Redis-based `already_sent` branch. That narrower window is still logically covered by the code (the `already_sent` branch itself repairs the DB status if it lags behind Redis) but was not independently exercised by a real crash today.
- **Scenario 2 — real DLQ + no head-of-line blocking:** a message whose mocked send always raises exhausted real retries (real backoff delays) and landed on the real `messages.outgoing.v1.dlq` topic with the real wrapped payload (confirmed by consuming it with an independent consumer). The DB row correctly ended up `FAILED`. A second ("canary") message published to the same real topic right after was still processed and reached `SENT` by the *same, still-running* consumer instance — proving the DLQ path doesn't stall the consumer on later records, real offset commit past the DLQ'd one included.
- **Scenario 3 — DLQ producer itself fails:** patched the real DLQ producer's `send_and_wait` (not `_publish_to_dlq` itself, to exercise the actual critical-log-then-re-raise code path in `kafka/consumer.py`) to raise, and confirmed `worker.run()` actually raises/exits rather than silently continuing past an unacknowledged record — the documented safety behavior in `kafka/consumer.py`'s module docstring was actually exercised, not just read.
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT cover:** the narrower Redis-then-DB race noted above; multi-partition/multi-replica rebalance behavior (the real topic has 1 partition); a real WhatsApp API failure mode (rate limiting, malformed webhook, etc.) — only a generic mocked exception; and the `ai_bot` sender path (`publish_pending_messages()` only sweeps `human_agent` rows — `ai_bot` replies are queued by a different, unverified-today code path per the routing worker).

### VCard real delivery + reminders — 2026-09-23

Item 7. Before this, the "gatekeeper" only flipped `Customer.vcard_state` and sent plain instructional text — no vCard file was ever built (the `vobject` dependency, added specifically for this, was never imported anywhere) and no reminder message was ever actually delivered (the reminder sweep only bumped DB state, with a `TODO(outbound)` admitting it).

- New [`vcard_builder.py`](omniflow-backend/src/ai_workers/vcard_gatekeeper/vcard_builder.py): builds a real VCard 3.0 file via `vobject` from the tenant's `business_name` and (new) `Tenant.whatsapp_display_phone_number`.
- **Product/schema decision made without waiting for sign-off** (additive, nullable, trivially reversible — not the kind of irreversible choice worth blocking on): added `Tenant.whatsapp_display_phone_number` ([migration `0010_tenant_wa_phone`](omniflow-backend/alembic/versions/0010_tenant_wa_phone.py)). The existing `Tenant.whatsapp_phone_number_id` is Meta's opaque internal ID, not a dialable number — a vCard built from it would be useless to the customer. **Needs follow-up:** nothing in onboarding captures this field yet, so every existing/new tenant's vCard will have no phone number until either onboarding is updated or it's backfilled manually. Flagging for your sign-off on where this field should be captured (onboarding flow vs. settings page).
- `WhatsAppClient` gained two real methods ([`client.py`](omniflow-backend/src/channel_adapters/whatsapp/client.py)): `upload_media()` (real multipart upload to Meta's Media API) and `send_document_message()`. Chose media upload over a public-URL-linked document specifically to avoid a dependency on item 16 (public storage), which isn't done — the vCard is generated fresh and uploaded directly, no storage bucket involved.
- `OutboundDispatcherWorker` now has a real `message_type == "vcard"` branch (previously vCard messages silently fell through to the plain-text default path and the attachment was dropped entirely).
- `vcard_tasks.advance_vcard_states()` now takes an optional `producer` and actually publishes the `reminder_1`/`reminder_2` nudge to the customer's real conversation (resolved via their most recent `Conversation` row), instead of only advancing the state machine. Backward compatible — omitting `producer` keeps the old (DB-only) behavior, which the existing `local_inbox_checks.py`/other tests rely on.
- **Verification, matching the item-6 bar** — real Postgres + real Kafka (Redpanda), through the real `VCardGatekeeperWorker`/`OutboundDispatcherWorker` consumer loops (`run()`/`_handle_record()`, not `process_message()` called directly). Only the WhatsApp network transport is faked, via `httpx.MockTransport` wired directly into `WhatsAppClient`'s real `httpx.AsyncClient` — the client's actual multipart-encoding and JSON-payload-building code runs unmodified and is what gets asserted on, not a mock of `send_text_message`/`upload_media` themselves. New script: [`scripts/validate_vcard_delivery.py`](omniflow-backend/scripts/validate_vcard_delivery.py), ran twice.
  - Scenario A: a real `NEW` customer, routed through a real `RoutingDecision` Kafka event, real gatekeeper worker advances DB state to `VCARD_SENT`; real dispatcher worker uploads a real vCard (asserted: valid `BEGIN:VCARD`, contains the tenant's actual business name and phone number) and sends a real `document`-type WhatsApp payload referencing it by media id, filename `contact.vcf`.
  - Scenario B: a customer manually backdated past the reminder threshold is swept by the real `advance_vcard_states()`, and the real reminder text is confirmed to actually reach the dispatcher and get sent — not just a DB flag flip.
- Full backend suite still passes (**37 tests**) unchanged.
- **What this does NOT prove:** a real phone receiving and being able to save this vCard — no WhatsApp test number/credentials exist yet (same gap as item 5). The reminder publish shares the same at-least-once risk as the human_agent path in `outbound_tasks.py` (publish before the sweep's transaction commits), but *without* a stable `message_id` for downstream dedup (each reminder gets a fresh random `source_event_id`) — an occasional duplicate reminder text is possible on a crash between publish and commit; accepted as low-severity for a nudge message, but noted honestly rather than silently carried over. Reminder de-duplication beyond the state-machine's own idempotency (a customer already moved out of `VCARD_SENT` won't be swept again) was not further hardened.

### Broadcasts/campaigns — real dispatch, delivery, and template gate — 2026-09-24

Item 8. `vip_subscribers`/`broadcast_deliveries` already existed (migration `20260813_srs_features` — the launch-readiness doc's premise that they were missing was stale). What was actually broken: `BroadcastWorker` used raw SQL against columns that don't exist at all (`broadcast_deliveries.status` instead of `delivery_status`, `vip_subscribers.list_type` which was never a real column, `customers.primary_phone` instead of `unified_phone`) and never called the WhatsApp API — `await asyncio.sleep(0.02)` with a comment claiming to "simulate" the send. It would have crashed on its first real recipient. There was also no Kafka-publish wiring from `schedule_campaign` (the router's own docstring admitted this) and no Meta template-approval gate anywhere.

- **Product decision made without waiting for sign-off** (per the "pick the simplest default matching the SRS and flag it" instruction): audience segmentation reuses the *existing* `all | vip | engaged | new | inactive` segments already implemented in `gateway/routers/broadcasts.py`'s `preview-audience` endpoint (VIP = `Customer.is_vip`, no separate named "lists"/tiers). This sidesteps the `vip_subscribers.list_type` scheme the old dead code assumed but never actually had a column for. **Needs your sign-off:** if the product actually wants multiple named VIP lists/tiers (not just a flat VIP flag plus engagement-based segments), that's a real, separate feature — the current `vip_subscribers` table (status/opted_in_at/opted_out_at/messages_sent_this_week) supports single-list opt-in tracking, not multi-tier membership.
- **The explicit ask — a pre-send Meta template-approval gate ("so free-text broadcast sends can't risk a number ban")** — implemented at two layers: `POST /{id}/schedule` now refuses (422 `TEMPLATE_REQUIRED`) to schedule a campaign without `meta_template_id`; the new `omniflow.broadcast_dispatch_check` Beat sweep (every minute) re-checks the same thing immediately before publishing to Kafka, since Meta can revoke template approval after scheduling. Sends now go through `WhatsAppClient.send_template_message` (already existed, was never called by anything) instead of free text.
- **Scheduling**: new Beat task `omniflow.broadcast_dispatch_check` finds `SCHEDULED` campaigns whose `scheduled_at` has arrived, publishes a real `BroadcastEvent` ([`shared/events/broadcast.py`](omniflow-backend/src/shared/events/broadcast.py), new) to `broadcast.marketing.v1`, and flips status to `SENDING` — closing the gap the router admitted ("wiring scheduled_at to an actual Kafka publish is a separate piece of work").
- **Delivery worker**: [`broadcast_worker/worker.py`](omniflow-backend/src/ai_workers/broadcast_worker/worker.py) rewritten from scratch on the ORM (no more raw-SQL column-name bugs), resolves the real audience per segment, sends a real template message per recipient (mocked network transport only, see below), rate-limited at 50/sec, records a real `BroadcastDelivery` row per recipient (`delivery_status`, real `platform_message_id`/wamid, `error_detail` on failure), and is idempotent against Kafka redelivery via the table's own `(campaign_id, customer_id)` unique constraint. Reuses `outbound_dispatcher`'s `_resolve_tenant_credentials` rather than duplicating tenant-credential resolution.
- **Delivery tracking**: only `SENT`/`FAILED` at send time is covered. `DELIVERED`/`READ` status updates depend on Meta's outbound status-callback webhook, which **does not exist anywhere in this codebase for any message type**, not just broadcasts — checked `gateway/routers/webhooks.py`, which only handles Clerk auth events. This is bigger than item 8 (it affects every WhatsApp send in the product, including item 6's human/AI messages) and wasn't built here; `broadcast_deliveries.delivered_at`/`READ` status will stay unset until that webhook exists.
- **Verification** — same bar as items 6/7: real Postgres + real Kafka, through the actual `_broadcast_dispatch_check_async()` task body and the real `BroadcastWorker.run()`/`_handle_record()` loop, only the WhatsApp network transport faked via `httpx.MockTransport`. New script: [`scripts/validate_broadcast_delivery.py`](omniflow-backend/scripts/validate_broadcast_delivery.py), ran twice. Scenario A: a template-less campaign is rejected by the real sweep before ever reaching Kafka. Scenario B: a VIP-segment campaign is dispatched, consumed, and sent to exactly the VIP-flagged customer (not the non-VIP one also in the DB), with a correctly-recorded delivery row. Full backend suite (37 tests) still passes.
- **What this does NOT cover** (real SRS §5.6 gaps, not silently claimed as done): the 3-messages/week frequency cap (`messages_sent_this_week` column exists, nothing increments or enforces it); opt-out keyword handling (no inbound handler flips `vip_subscribers.status`); the 90-day re-ask cooldown on decline; per-recipient template parameter substitution (`send_template_message` is called with no `components`, so a template with `{{1}}`-style placeholders would send them literally — fine for a static-text template, not for a personalized one); and, as above, `DELIVERED`/`READ` tracking.

### Real RAG embeddings + verified tenant isolation — 2026-09-24

Item 9. Retrieval was disabled outright (`rag_unavailable_no_embedding_provider`) whenever no OpenAI key was configured. The alternative was never "no RAG" in the code, though — `embedder.py` had a `random.seed(text)`-based mock-vector path that was semantically meaningless, and the team correctly chose to disable retrieval rather than run on it. What was actually missing: `fastembed`/`sentence-transformers`/`qdrant-client[fastembed]` were already project dependencies for exactly this purpose (real, local, no-API-key embeddings) and were never imported or wired into any code path — not a fundamental blocker, just unfinished wiring, plus `fastembed` wasn't even installed in the local dev venv despite being declared.

- `embedder.py` rewritten to support two real providers: OpenAI (`text-embedding-3-small`, 1536-dim) when a real key is configured, otherwise local `fastembed`/`BAAI/bge-small-en-v1.5` (384-dim, ONNX, CPU, no API key or ongoing network dependency — only a one-time model download cached to disk). The random-vector mock is gone entirely; there is no more "no embedding provider" state.
- `shared/core/config.py` gained `embedding_provider`/`embedding_dim` computed properties as the single source of truth for which provider is active and its vector size — `shared/qdrant_client/client.py`'s `EMBEDDING_DIM` (previously its own separately-hardcoded `1536` constant, a second latent source of drift) now reads the same value, so the two can never disagree again.
- `llm_invoker/worker.py`'s `_rag_enabled` gate removed — RAG is now always enabled, since a real embedding path always exists.
- **Verified with a real, running Qdrant** (docker-compose service) and the real local fastembed model — no mocks. New script: [`scripts/validate_rag_embeddings.py`](omniflow-backend/scripts/validate_rag_embeddings.py), ran three times (real model download/cache confirmed working; one run left two Qdrant collections uncleaned after an unrelated assertion failure during script iteration — deleted manually and confirmed the underlying `delete_collection` call works correctly when invoked directly, so this looks like a one-off timing issue during my own iteration, not a reproducible bug in the cleanup path, but flagging it rather than asserting certainty).
  - Real tenant isolation, empirically: two tenants' listings ingested into their own real per-tenant Qdrant collections; a real estate query against each tenant returns *only* that tenant's listing, never the other's.
  - Real semantic signal, not just structural isolation: the same query scores measurably higher against a relevant listing than against a semantically unrelated one (0.853 vs 0.832 cosine similarity) — the vectors carry genuine meaning, not noise.
  - The real production code path — `RAGRetriever.get_rag_context()`, the actual function the L2 routing tier calls — returns a real, correctly-formatted context string built from the real retrieved listing.
- Full backend suite (37 tests) still passes.
- **What this does NOT cover:** the Instagram-path isolation concern the item specifically named was investigated by inspection (Instagram's `_resolve_tenant_id(page_id)` feeds the same canonical event → `RoutingDecision` → RAG code path every other channel uses, no separate/leaky code path exists) but not exercised by a real Instagram message end-to-end — no Instagram test credentials exist yet. Retrieval quality with the smaller local 384-dim model vs. OpenAI's 1536-dim embeddings was not benchmarked — fastembed is real and semantically meaningful, not necessarily equally accurate; a tenant with an OpenAI key still gets the higher-quality provider automatically. Production Docker images need to either bake in the fastembed model files or accept a one-time ~30s download on first startup — not yet reflected in the Dockerfile.

### Real-estate recommendation panel — 2026-09-24

Item 10. The inbox context panel's recommendations array was a hardcoded empty list with no backend behind it at all. While wiring it up, found and fixed a more consequential bug in the same area: `shared/services/vector_sync.py` — the function the *properties API actually calls* whenever a real listing is created or updated — had its own independent, undocumented-outside-a-comment embedding implementation that fell back to a **constant** dummy vector (`[0.1] * 1536`, identical for every listing) whenever no OpenAI key was configured. That's worse than item 9's random-vector mock: every real listing would have looked equally "relevant" to any query, silently poisoning both AI-facing RAG and this new recommendation panel. Fixed by deleting that duplicate implementation and routing it through the same `EmbeddingService` singleton item 9 already fixed — one real embedding implementation now, not three independent ones that could each drift.

- **Product/design decision made without waiting for sign-off** (per the "pick the simplest default matching what exists, flag it" instruction — there's no SRS spec for this feature at all, it doesn't appear in either SRS document): new `GET /api/v1/conversations/{id}/recommendations` endpoint builds its Qdrant query from the conversation's own last 5 **customer** messages (concatenated), with no separate LLM/rule-based preference-extraction step. `score` is Qdrant's raw cosine similarity, not a bespoke weighted match-rule score. **Needs your sign-off:** if the product wants an explicit "extracted budget/area/bedrooms" step (the `intent`/`budget`/`lookingIn`/`propertyType` fields already declared in the frontend's `Conversation` type but never populated by anything — a related, pre-existing, separate gap this item does not fix), that's real additional work, not a config toggle on top of this.
- Frontend: `CustomerContext.tsx`'s hardcoded `[]` replaced with a real fetch (`lib/api/inbox.ts::fetchRecommendations`) on conversation change; empty-state text still shows correctly when a conversation has no customer messages yet or nothing scores above zero.
- **Verified with real Postgres + real Qdrant + real fastembed**, through the actual `vector_sync.sync_listing_to_qdrant` ingestion function (the same one the properties API calls) and the actual `get_conversation_recommendations` endpoint function — real dependencies constructed manually (real DB session, real repository), not the full FastAPI/HTTP/Clerk-auth layer, which is an acknowledged scope reduction, stated plainly rather than implied as full coverage. New script: [`scripts/validate_recommendations.py`](omniflow-backend/scripts/validate_recommendations.py), ran twice — a real listing ("apartment, Narjis, 1.15M SAR, 210m²") is ingested via the real production embedding path and correctly retrieved and formatted (score 0.858) by the real endpoint against a conversation whose customer wrote "أبحث عن شقة 3 غرف في حي النرجس بالرياض". Frontend: `tsc --noEmit`, `eslint` (0 warnings on the changed files), full Next.js production build, and the existing 12-test frontend suite all pass. Backend suite (37 tests) still passes.
- **What this does NOT cover:** the extracted-preferences fields (`budget`/`lookingIn`/`propertyType`/`intent`) shown elsewhere in the same panel remain unpopulated — a separate, pre-existing gap, not part of this item; no A/B or quality evaluation of "query = recent customer messages" vs. a proper extraction step; the endpoint was not exercised through a real HTTP request with real Clerk auth end-to-end.

### Inbox quick actions: notes, appointments, reports link — 2026-09-24

Item 11. All three buttons were permanently `disabled` with a single static "not available" sentence — no click handler, no backend, no comment explaining intent.

- **Reports** ("إنشاء تقرير عميل" → relabelled "عرض تقارير العميل"): the only place a `CustomerReport` row is ever created is the paid-report generator triggered by a completed Moyasar/Tap transaction (item 3, not built yet) — a literal "generate now" button would either do nothing real or misleadingly imply free report generation. **Product decision made without waiting for sign-off**: wired as a plain link to the existing reports page instead of a fake generate action. Not customer-filtered — the frontend `Conversation` type has no `customerId` field (only `customerPhone`), and plumbing one through was judged out of scope for this item; flagged here rather than silently left as a smaller-than-ideal feature.
- **Internal notes** ("إضافة ملاحظة تحذير"): new `conversation_notes` table + `POST`/`GET /api/v1/conversations/{id}/notes`, real end-to-end — an agent can now attach an internal, customer-invisible note (e.g. flagging a difficult customer) and see it persisted.
- **Viewing appointments** ("جدولة موعد زيارة"): new `appointments` table + `POST`/`GET /api/v1/conversations/{id}/appointments`. **Deliberate, flagged scope cut**: the SRS (§5) describes a full subsystem here — distance-based dispatch routing (OSRM/Google Distance Matrix), CalDAV/Google Calendar/Outlook sync per consultant, conflict detection, 30-minute buffers, 24h/1h automated reminders. None of that is built. What exists is a structured date/time + free-text location note — genuinely real and working, but a fraction of the SRS vision. **Needs your sign-off**: is this minimal version acceptable for launch, with calendar sync/routing/reminders as explicit post-launch work, or is a bigger investment here required before launch?
- Both new tables follow the existing RLS convention exactly (`tenant_isolation_policy` + `system_bypass_policy` per the maintenance contract in migration `0006_system_bypass_policy.py` — a new tenant-scoped table missing the bypass policy silently returns zero rows to `get_system_session()` callers). Migration: [`0011_notes_appointments`](omniflow-backend/alembic/versions/0011_notes_appointments.py).
- **Verified with real Postgres**, through the actual endpoint functions (`create_conversation_note`, `list_conversation_notes`, `create_appointment`, `list_appointments`) with a real `ConversationRepository` bound to a real tenant-scoped session — the full FastAPI/HTTP/Clerk-auth layer itself was not exercised, stated plainly rather than implied as full coverage. New script: [`scripts/validate_quick_actions.py`](omniflow-backend/scripts/validate_quick_actions.py), ran twice: a note is created and correctly read back with the right author/severity; a past-dated appointment is correctly rejected (422); a valid appointment is created, correctly inherits the conversation's real `customer_id`, and reads back correctly.
- Frontend: the three buttons now do real things — the reports link navigates, and the note/appointment buttons expand an inline form (no modal library pulled in) that calls the real endpoints and shows inline success/failure feedback. `tsc --noEmit`, `eslint` (0 new warnings), full Next.js production build, and the existing 12-test frontend suite all pass. Backend suite (37 tests) still passes.
- **What this does NOT cover:** any HTTP-level test of the new endpoints (real Clerk auth, real FastAPI request/response cycle); the reports link isn't customer-filtered (see above); no UI for editing/deleting a note or cancelling an appointment — create + list only; no calendar sync, distance routing, or reminders for appointments, as flagged above.

### Long chat pagination + real-time delivery status (item 12) — 2026-09-24

Two distinct sub-problems, both real bugs beyond just "missing features."

**Pagination — was actively hiding recent messages, not just "no infinite scroll."** `GET .../messages` already supported `offset`/`limit`, but the frontend called it with no params, defaulting to `offset=0, limit=100` — for any conversation past 100 messages, the customer's actual newest messages were silently never fetched at all (hidden behind the oldest page), while the UI gave no indication anything was missing.

- Backend: replaced offset pagination for this endpoint with cursor-based pagination — new `ConversationRepository.get_recent_messages(limit, before=None)`. No `before` now returns the **most recent** `limit` messages (oldest-first for direct rendering); passing the oldest loaded message's `created_at` as `before` pages further into the past. `PaginatedMessages` gained `has_more` (a full-page heuristic, not an exact `COUNT(*)` — a false positive just costs one harmless extra click that returns `[]`). The older offset-based `get_messages()` repository method is untouched and still used by the recommendations endpoint (item 10), which doesn't need this fix.
- Frontend: `inboxStore` now loads the tail by default and has a real `loadOlderMessages()` thunk; `ChatArea.tsx` gained a "تحميل رسائل أقدم" button (a plain button, not scroll-triggered infinite scroll — simpler, and scroll-position preservation around it is handled explicitly rather than fighting an IntersectionObserver) that preserves the agent's scroll position when older messages are prepended, instead of the auto-scroll-to-bottom effect yanking the view (that effect now only fires when a message is *appended*, detected via the oldest-message-id staying the same).

**Delivery status — the data already existed, was already flowing to the frontend, and was rendered... backwards.** `Message.deliveryStatus` was already mapped from the API response, but `ChatArea.tsx` only showed a checkmark for **customer** messages when `isRead` was true — "the agent read the customer's message," the opposite of what item 12 asks for (showing OUR outbound delivery status to the customer). Separately, `update_message_delivery_status()` (called whenever a message's status changes after creation, e.g. QUEUED → SENT) never published anything — an already-open inbox would show a message's *original* status forever until a full page reload.

- Backend: `update_message_delivery_status()` now publishes a real `message_status_update` SSE event (same Redis pub/sub mechanism as `new_message`/`conversation_update` — the SSE generator forwards any event type with no hardcoded allowlist, so no gateway change was needed there).
- Frontend: new `message_status_update` SSE handler + `updateMessageStatus` store action patch the specific message in place; `ChatArea.tsx` now renders a real `DeliveryStatusIcon` (clock for PENDING/QUEUED, single check for SENT, double check for DELIVERED, blue double check for READ, warning icon for FAILED) on outbound messages.
- **What this does NOT change:** DELIVERED/READ will simply start appearing whenever the Meta status-callback webhook (confirmed, again, to not exist anywhere in this codebase — the same gap noted in items 6 and 8) gets built; today the pipeline can only ever actually reach PENDING/QUEUED/SENT/FAILED. This item made the plumbing and UI correct and ready for that day, not the webhook itself.
- **Verified with real Postgres (105 real messages in one conversation) and real Redis pub/sub** — the exact channel (`sse:{tenant_id}`) and payload shape the SSE endpoint actually uses — through the real `get_recent_messages`/`update_message_delivery_status` functions. New script: [`scripts/validate_inbox_pagination_and_status.py`](omniflow-backend/scripts/validate_inbox_pagination_and_status.py), ran twice: the no-cursor call returns the 100 newest messages (not the oldest 100); the `before` cursor returns exactly the remaining 5 with no gap or overlap; the two pages together cover all 105 real messages exactly once; a real subscriber on the real Redis channel receives the real `message_status_update` event with the correct message/conversation ids after a real DB update.
- Found and fixed a real regression while doing this: an existing frontend test (`inbox-store.test.cjs`) mocked the old `fetchMessages` contract (a bare array) against the new `{items, hasMore}` return shape — updated the test rather than papering over the contract change.
- Frontend: `tsc --noEmit`, `eslint` (no new warnings beyond one pre-existing pattern already used elsewhere in the same file for other Zustand actions), full Next.js production build, and the full 12-test frontend suite all pass. Backend suite (37 tests) still passes.

### Concurrency audit — conversation counters, takeover races, SSE ordering (item 13) — 2026-09-24

Audited every write path touched by two agents/workers racing on the same row: `message_count`, agent takeover, user provisioning, SSE publication timing, and the AI-reply-vs-human-takeover window. Four real races confirmed, two already safe, one narrowed but not closed.

**1. `Conversation.message_count` — real lost-update race, fixed.** `ConversationRepository.add_message()` did `conv.message_count = (conv.message_count or 0) + 1` in Python (read-modify-write): two concurrent inserts on the same conversation could both read the same starting value and both write `N+1`, silently losing one increment. Replaced with a single atomic `UPDATE conversations SET message_count = message_count + 1 WHERE conversation_id = :id` — the increment now happens inside Postgres, not in application code. `Customer.engagement_score` was also audited: no writer exists anywhere in the codebase (only read in `broadcast_worker`, `tasks.py`, `dashboard.py`), so there is no race to fix there — it's simply unimplemented.

**2. Takeover/ownership double-assign — real race, fixed.** `ConversationRepository.assign_agent()` used to read `assigned_agent_id`, then unconditionally write the caller's id — two agents clicking "take over" within milliseconds could both read `NULL` and both write, with whichever commit landed last silently winning; the loser got a false 200 OK and only discovered the truth on their next `send_message` (a 409). Replaced with a single atomic conditional `UPDATE ... SET assigned_agent_id = :agent, status = 'human_active' WHERE assigned_agent_id IS NULL OR assigned_agent_id = :agent`. Postgres itself is now the arbiter: at most one concurrent UPDATE can match on an unassigned row. A new `ConversationConflictError` is raised when `rowcount == 0` and the conversation is genuinely owned by someone else (distinguished from "not found" via `get_or_404`), mapped to a real HTTP 409 `{"code": "ALREADY_ASSIGNED"}` by a new exception handler in `gateway/main.py`.

**3. SSE published before the transaction commits — real race, fixed.** `gateway/routers/conversations.py`'s three endpoints (`send_message`, `takeover_conversation`, `return_to_ai`) called `_publish_sse_event()` inline in the endpoint body — before the FastAPI dependency-injected session's surrounding `async with get_tenant_session(...)` block actually commits (which only happens when the dependency generator tears down, after the endpoint function returns). A subscriber could receive an event for a row a concurrent fresh read still couldn't see. Fixed by moving all three publishes onto FastAPI `BackgroundTasks`, which Starlette only runs after the response has been prepared — strictly after the session dependency's commit. (Note: `persistence.py`'s own SSE call sites for `persist_inbound_message`/`persist_outbound_message`/`update_message_delivery_status` were already audited and confirmed correct — they already sit after their own `get_tenant_session` block exits.)

**4. AI-reply-after-takeover — real TOCTOU window, narrowed, not closed.** `outbound_dispatcher/worker.py` checked `load_conversation_state()` once, then had a real async gap (credential resolution, Redis idempotency lookup, a DB persist) before actually calling the WhatsApp send — if a human took over during that gap, a stale AI reply could still go out. Added a second `load_conversation_state()` re-check immediately before the network call, narrowing the window to just the WhatsApp API round-trip itself. **This is not a full fix** — a complete fix needs a lock or fencing token on the conversation, which was judged out of scope for this pass. `semantic_router/worker.py` and `llm_invoker/worker.py` use the same one-shot check pattern upstream of routing/generation, not upstream of the actual send, so a stale routing/generation decision there just produces a queued outbound message that still has to pass through `outbound_dispatcher`'s (now double) check before anything reaches WhatsApp — left unchanged.

**5. Already safe, no change needed.** User-provisioning races (`webhooks.py::_handle_user_created`) already use an optimistic insert inside a `SAVEPOINT`, relying on a real DB-level `unique=True` constraint on `TenantUser.clerk_id`; the losing request's `IntegrityError` is caught and it returns a benign "already provisioned" 200.

**Verified with real concurrent Postgres connections (not asyncio tasks sharing one session) and real Redis pub/sub** — new script: [`scripts/validate_concurrency_races.py`](omniflow-backend/scripts/validate_concurrency_races.py), ran twice. Scenario A: 8 genuinely concurrent `assign_agent` calls (each its own connection, real `TenantUser` FK rows) on the same unassigned conversation — exactly 1 wins, the other 7 get a real `ConversationConflictError`, and the DB ends up owned by exactly one agent. Scenario B: 20 genuinely concurrent `add_message` calls — `message_count` ends up exactly 20, matching the real row count (a lost update would show fewer). Scenario C: the real `takeover_conversation` endpoint function with a real `BackgroundTasks` — confirms a real Redis subscriber receives **nothing** while the transaction is still open, and only receives the event (with the row already durably committed and visible on a fresh read) after the background task is actually run.

**Not covered by the validation script** (explicitly, not silently): the item-4 TOCTOU narrowing in `outbound_dispatcher` needs a real Kafka + WhatsApp-transport-boundary harness to exercise end-to-end under actual concurrent load; it was verified by code inspection and the existing 37-test unit suite (all still passing) only, not by a dedicated real-dependency race drill.

Backend suite: 37/37 tests still pass after all four fixes.

### Voice/image — explicit v1 disablement, not a silent gap (item 14) — 2026-09-24

Audited both pipelines end to end. **Outbound voice (TTS replies) was already fully working** — `TTSClient` uses real ElevenLabs when configured, and `Settings.validate_production_settings()` already refuses to boot in production if `feature_voice_notes_enabled=True` with no real key. No change needed there.

**Inbound voice (speech-to-text) and image (vision/OCR) were a real, undisclosed gap, not "unclear."** `ai_workers/multimodal/__init__.py` is a docstring-only stub — no Whisper worker, no vision worker, nothing ever consumes the `multimodal.audio.v1` / `multimodal.vision.v1` topics referenced only by name in config. Before this fix, an uncaptioned voice note or image fell through to `LLMInvokerWorker._build_current_message()`'s placeholder branch, which handed the LLM a string like `"[audio message — media: waid:123]"` as if it were the customer's actual message — the LLM then generated a confident, plausible-sounding reply to content it never saw. No error, no warning, no signal to the customer or to us that anything was wrong.

Per the SRS (§4.4.2/§4.4.3), building real Whisper + Vision-LLM pipelines is promised as a marketed feature, but doing so needs a real OpenAI/Whisper key, a real vision-LLM key (GPT-4o or Gemini 1.5 Pro), and a real GPU worker for self-hosted Whisper — none of which exist yet and none of which are safe to fake. Per the standing instruction to pick the simplest default matching the SRS and flag it rather than stall, and per the item's own wording ("either finish both pipelines properly or explicitly, visibly disable them for v1"), the decision made here is: **explicitly disable inbound voice/image AI processing for v1**, flagged for sign-off below.

- `Settings.feature_multimodal_voice` / `feature_multimodal_vision` flipped from `True` (previously unused/decorative — nothing read them) to `False` by default, and now actually wired.
- `LLMInvokerWorker.process_message()` gained a guard immediately after the existing L0/HUMAN fast paths: an audio/image/video message with **no caption** and its corresponding feature flag off now short-circuits to a canned Arabic reply ("عذرًا، لا يمكنني حاليًا الاستماع للرسائل الصوتية أو تحليل الصور تلقائيًا...") published as a normal text `OutboundMessage`, and the LLM is never invoked. A message that **does** have a caption (e.g. a voice note transcript entered as WhatsApp's own caption, or an image with a text caption) is unaffected and takes the normal LLM path — this only closes the blind/silent case.
- **Needs user sign-off**: this is a product-visible behavior change (customers who send a voice note or bare image now get an explicit "please type your question" reply instead of a made-up AI answer) and a SRS-scope deferral (real STT/vision is a marketed §4.4.2/§4.4.3 feature, not shipped for v1). Flagging per the standing instruction rather than blocking on it.
- **Verified with real Postgres + real Kafka (Redpanda)**: new script [`scripts/validate_multimodal_fallback.py`](omniflow-backend/scripts/validate_multimodal_fallback.py), ran twice. Calls the real `LLMInvokerWorker.process_message()` (not a reimplementation of the guard) with a real `RoutingDecision`/`CanonicalInboundEvent`, and reads the actual `OutboundMessage` back off the real `messages.outgoing.v1` topic. `gemini_client.generate_response` is monkeypatched to raise (a tripwire, not a business-logic mock) so the script fails loudly if the guard is ever bypassed. Scenario A: uncaptioned audio -> real fallback text on the real topic, `model_used="L0_deterministic"` (LLM never called). Scenario B: same for an uncaptioned image. Scenario C: audio **with** a caption correctly still reaches the real LLM call path (tripwire fires as expected) — proving the guard doesn't over-match ordinary captioned media.
- **Not covered**: this only prevents the silent-hallucination failure mode. It does not build a real STT/vision pipeline — that remains fully unimplemented and out of scope for v1 per the above.

### REGA license/ad-number verification (item 15) — audited, genuinely blocked — 2026-09-24

Audited the full path: `PropertyListing.rega_ad_number`/`is_verified` fields exist, `ConversationRepository.mark_verified()` exists to flip a listing to `VERIFIED_ACTIVE` — but it has **zero callers anywhere in the codebase**. There is no REGA HTTP client, no verification Celery task, nothing that ever calls out to REGA at all. `feature_rega_verification` only gates a staleness-*re*-check sweep (flips old verified listings back to `PENDING_VERIFICATION` after 30 days) — it never performs the initial forward verification. `gateway/routers/properties.py` actively works around this today by auto-generating a `DEV-REGA-{hex8}` placeholder ad number when none is given, explicitly to avoid blocking development.

`rega_api_base_url` defaults to `https://api.rega.gov.sa` and the SRS's own "Contract C" example cites `POST /v2/advertisements/verify` with `Authorization: Bearer <REGA_API_TOKEN>` — but this could not be confirmed against any real, public REGA API documentation, sandbox, or partner agreement anywhere in this repo or its docs. There is no evidence this is a real, reachable endpoint versus an illustrative placeholder invented for the SRS example.

**Not attempted**: building a "verification" client against a guessed URL with no real credentials would produce code that either always fails (useless) or is never actually exercised against the real service (unverifiable, and risks silently shipping a broker-facing feature that quietly does nothing — exactly the kind of unverified claim this document exists to prevent). This is genuinely blocked on the user obtaining real REGA API/partner access (URL, auth token, and confirmed request/response contract).

### Dependency pinning, vulnerability scan, image size (item 19) — 2026-09-24

**Pinning.** `pyproject.toml` kept `>=` floors for local dev flexibility (unchanged), but nothing pinned what the Docker image actually installs — a build today and a build next month could silently resolve different transitive versions. Added [`omniflow-backend/requirements-lock.txt`](omniflow-backend/requirements-lock.txt): all 265 runtime dependencies pinned to the exact versions already tested against (the working `.venv` that passes the 37-test suite), generated via `pip freeze`, with Windows-only transitive packages (`pywin32`, `win32_setctime` — would break the Linux image build) and dev-only tools filtered out. `torch` is deliberately excluded from the lock file and pinned separately in the Dockerfile (`torch==2.13.0+cpu` from the CPU-only index), since it was already special-cased there.

**Vulnerability scan.** Ran `bandit` against `src/` (real scan, real findings, not skipped): 1 MEDIUM (`B104`, intentional `0.0.0.0` bind inside a container — not a real issue), 22 LOW (mostly `assert` usage and 3 false-positive "hardcoded password" hits on literal strings like `"clerk_managed"`/`"access"`/`"refresh"` that are sentinel/token-type values, not secrets). Ran `pip-audit` against the pinned lock file — found and fixed 2 of 3 real CVEs:
- `PyPDF2==3.0.1` (infinite-loop DoS, `PYSEC-2026-1835`) — confirmed via grep it's never imported anywhere in `src/`, a dead leftover from before the PDF pipeline moved to WeasyPrint. Removed entirely from `pyproject.toml` and the lock file, rather than upgrading dead code.
- `pypdf==6.15.0` (3 DoS CVEs) — this one **is** actually used (`ai_workers/rag_engine/ingestion.py:129`). Bumped to `6.16.1` (the patched version), reinstalled, and the full 37-test backend suite still passes.
- `ecdsa==0.19.2` (Minerva timing-attack CVE, **no fix version exists upstream**) — pulled in transitively by `python-jose` (which we do use, for HS256/RS256 JWTs). Confirmed via `src/shared/security/jwt.py` that ECDSA algorithms are never actually used anywhere in this codebase (only HS256 and RS256) — the vulnerable `ecdsa.SigningKey.sign_digest()` code path is dead weight in our dependency tree, not a live attack surface. Left as a documented, accepted, currently-unfixable transitive dependency.

**Image size.** Baseline confirmed: `omniflow-backend:readiness-validation` = 6.89 GB. Rewrote the Dockerfile as a multi-stage build — `build-essential`/`libffi-dev` (compilers, dev headers) now only exist in a discarded `builder` stage, never reach the runtime image. Also dropped `libpq-dev`/`libpq5` entirely (the app uses `asyncpg`, a pure-async driver — confirmed via `alembic/env.py` and grep that no `psycopg`/libpq-dependent driver exists anywhere) and `torchvision` (installed but never imported anywhere in `src/`, and not even present in the actual tested dev venv). **Verified with a real `docker build` + real `docker run`**, not just a Dockerfile edit: new image = **5.62 GB (–1.27 GB, –18%)**. The remaining size is legitimately the ML/document stack itself (torch, transformers, sentence-transformers, `unstructured[all-docs]`, camel-tools) that the product actually needs at runtime — a multi-stage build can only remove build-time tooling and genuinely dead weight, not shrink libraries that are actually used. Smoke-tested the new image directly: `docker run` with real env vars imports `pypdf`/`weasyprint`/`torch`/`fastembed` successfully, confirms `PyPDF2`/`torchvision` are correctly absent, boots the real Uvicorn server, connects to real Redis/Kafka containers on the compose network, and a real `curl` against `/health` returns `{"status":"ok",...}`.

**Base image pin.** While rebuilding, discovered `python:3.11-slim` (a floating tag) had rolled to Debian "trixie," which renamed `libgdk-pixbuf2.0-0` and broke the apt install outright — an unrelated, pre-existing fragility this session's build happened to surface. Pinned both build stages to `python:3.11-slim-bookworm` explicitly so the base OS can't shift under the build again.

**Not covered**: `safety`'s commercial vulnerability DB wasn't used (only the free `pip-audit`/OSV data) — this is unlikely to matter (both draw from overlapping public CVE sources) but is worth noting as unverified against safety's own DB specifically.

### CI/CD pipeline (item 20) — 2026-09-24

`omniflow-deploy/RELEASE_CHECKLIST.md` documented manual local commands only (`unittest discover`, `node --test`, `npm run lint/build`) with no automation running them on push/PR — confirmed via `find`/`ls`, no `.github/workflows/` or any other CI config existed anywhere in the repo before this.

Added [`.github/workflows/ci.yml`](.github/workflows/ci.yml): two jobs on every push/PR to `main`.
- **backend**: installs the exact pinned dependency set from item 19 (`torch==2.13.0+cpu` from the CPU index, then `requirements-lock.txt`, then the local package with `--no-deps` — the same three commands the Dockerfile now uses, so CI and the production image install identically), runs `bandit` and fails the build only on a new HIGH-severity finding (the 1 known MEDIUM false-positive and 22 LOW findings from item 19 don't block), runs `pip-audit` as a report-only step (the one unfixable `ecdsa` CVE shouldn't block every build), then runs the real 37-test `unittest` suite.
- **frontend**: `npm ci`, `eslint`, `tsc --noEmit`, the real `node --test` suite (12 tests), then `next build` (with placeholder Clerk/API env vars — a real build-time key isn't needed to prove the build succeeds).

**Verified for real, not just written**: every command in both jobs was actually run locally against this repo before being put in the workflow — `bandit`/`pip-audit`/the gate script exit 0 as expected, `pip install --no-deps -e .` succeeds, and separately `npm run lint` (0 errors, pre-existing warnings only), `npm run type-check` (clean), `node --test` (12/12 pass), and `npm run build` with placeholder env vars (succeeds) were all run directly, matching exactly what the workflow YAML executes. The YAML itself was parsed with PyYAML to confirm it's syntactically valid.

**Not covered**: this does not run the `scripts/validate_*.py` real-dependency drills (Kafka/Postgres/Redis/Qdrant integration tests) in CI — those need service containers wired up (Postgres, Redpanda, Redis, Qdrant) as a separate, heavier job, which is real additional work beyond this pass. The workflow has never actually executed on GitHub's runners (no push to trigger it happened as part of this session) — local verification of each exact command is the evidence here, not a green Actions run.

### File/storage: retention sweep implemented, addressing/signed-URL audit (item 16) — 2026-09-24

**Real domain / public storage addressing**: still genuinely blocked — no real domain exists yet, so links necessarily point at the local MinIO endpoint. Not attempted here.

**Signed-URL coverage audit**: checked every `upload_file()` caller. Reports (`report_download.py`) already store just the S3 key and generate a fresh presigned URL per download — correct. Knowledge documents (`knowledge.py`) also already store just `s3_key` in the DB and never expose a raw or signed URL to a client at all today (only an internal ingestion worker reads the bytes server-side) — no gap. Tenant logos (`settings.py`) store a raw, non-presigned URL — **this is correct, not a bug**: `s3_assets_bucket`'s own docstring in `config.py:169-170` says it's the public bucket, and a logo needs a stable long-lived URL for repeated rendering, not one that expires every 15 minutes. No code change needed for signed-URL coverage.

**Retention/cleanup — real, confirmed gap, now fixed.** `media_voice_retention_hours`/`media_image_retention_days` were declared in `config.py` but had zero enforcement anywhere — `celery_app/__init__.py`'s own docstring listed "Media cleanup (voice 24h / images 7d)" under "Still to build." Added a real hourly Celery beat task, `omniflow.media_cleanup_check`, backed by a new `StorageClient.delete_expired_objects()` that lists real objects via `list_objects_v2` and deletes ones older than the retention window. Scoped to voice only: per item 14's audit, no image is ever downloaded into our own storage at all (WhatsApp images are only ever referenced by their platform-hosted `media_url`), so `media_image_retention_days` has nothing to sweep yet — documented in both the task's docstring and `celery_app/__init__.py`, not silently ignored.

**Real bug caught by the validation drill, not assumed away**: the first implementation used the batch `delete_objects` API, which this MinIO deployment rejected with `MissingContentMD5` — a real botocore/MinIO version compatibility gap, only found because the script ran against a real bucket instead of a mock. Fixed by switching to per-object `delete_object` calls (fine at this sweep's real volume — TTS audio only).

**Verified against real MinIO** (not mocked): new script [`scripts/validate_media_retention.py`](omniflow-backend/scripts/validate_media_retention.py), ran twice. Uploads real objects to the real `tts/` prefix, runs the real `delete_expired_objects()` with an effectively-zero-hour cutoff (MinIO stamps `LastModified` as upload time — no way to backdate client-side — so the test proves the real mechanics with a tiny window instead of waiting 24 real hours): the stale object is deleted, a freshly-reuploaded object survives, and an object outside the swept prefix is untouched. Also runs the real `omniflow.media_cleanup_check` task body directly against real production data with the real 24h setting, confirming it's a true no-op today (nothing in `tts/` is actually 24h old yet) rather than mocking the task.

### OpenTelemetry tracing wired and proven; Sentry and stack deployment remain blocked (item 17) — 2026-09-24

Audited both halves of "settings alone aren't instrumentation" separately, since they turned out to have very different blockers.

**Sentry: genuinely blocked, zero engineering possible.** `sentry_dsn` is a config field with nothing else — no `sentry_sdk.init()` anywhere in `src/`, and `sentry-sdk` isn't even an installed dependency. There is nothing to build without a real DSN from a real Sentry account/project. Not attempted.

**OpenTelemetry: was a pure code gap, not blocked — fixed.** `shared/observability/tracing.py` was a 3-line docstring stub, and the one call site in `gateway/main.py` was commented out under a "Future (Sprint 6+)" heading — the SDK skeleton (`opentelemetry-api`/`-sdk`) was installed but nothing exported or instrumented anything. Separately, a real local OTLP collector (Jaeger) was already defined in `docker-compose.yml` under `profiles: ["monitoring"]`, publishing OTLP gRPC on `:4317` — already matching the default `otel_exporter_otlp_endpoint` — just never started or pointed at.

- Implemented real `setup_opentelemetry(settings, app)`: a `TracerProvider` + `OTLPSpanExporter` (gRPC) + `BatchSpanProcessor`, with `FastAPIInstrumentor`/`SQLAlchemyInstrumentor`/`RedisInstrumentor` auto-instrumentation. Wired into `gateway/main.py`'s lifespan (uncommented and implemented the call, not just re-enabled a dead stub).
- **Not instrumented**: aiokafka. There is no upstream `opentelemetry-instrumentation-aiokafka` package — the one that exists instruments `kafka-python`, a different client library this codebase doesn't use. `pyproject.toml` previously (wrongly) declared `opentelemetry-instrumentation-kafka-python`, which would have installed cleanly and instrumented nothing; removed it and documented why in both `pyproject.toml` and `tracing.py`'s docstring rather than leaving a dependency that silently does nothing.
- **Verified against a real local Jaeger container**, not a mock: new script [`scripts/validate_otel_tracing.py`](omniflow-backend/scripts/validate_otel_tracing.py), ran twice. Boots the actual `create_app()`/lifespan (which calls the real `setup_opentelemetry`), makes a real HTTP request through a real ASGI transport, force-flushes the real span processor, then queries **Jaeger's own real HTTP API** and confirms a real span for that exact `GET /health` request actually landed — not just that the exporter initialized without raising.
- Also discovered and fixed a real, unrelated bug while auditing this: `pyproject.toml` had drifted from what's actually installed in the tested dev environment (`pip install --no-deps -e .` reported `anthropic`, `camel-tools`, `celery-redbeat`, `flower`, `langchain`, `langchain-community`, `sentence-transformers`, `transformers`, `unstructured[all-docs]` as declared-but-not-installed) — flagged here as a gap worth a dedicated cleanup pass, not fixed in this change since it's outside item 17's scope and installing the full ML stack is a large, slow operation better done deliberately.

**Monitoring stack deployment: still blocked, but for a different reason than before.** `docker-compose.monitoring.yml` (Prometheus/Grafana/postgres-exporter/redis-exporter) genuinely needs a real target host to "deploy" to — there's nothing to build locally here beyond what already exists; this is an infrastructure/hosting decision tied to item 21 (no production host/domain yet), not a code gap.

### Real Qdrant/MinIO restore drills (item 18, partial) — 2026-09-24

**Off-host backup storage: still blocked** — needs real cloud credentials (S3/GCS bucket + keys) that don't exist yet. Not attempted.

**Restore drills: real, confirmed gap, now closed for Qdrant and MinIO.** `RELEASE_CHECKLIST.md` already said this plainly: "PostgreSQL restoration was tested locally in a disposable container; off-host copies and Qdrant/MinIO restoration remain unverified." Audited `backup.sh`: it already had real backup code for all three systems, but Qdrant's restore procedure existed only as a comment (`docker cp` + a curl command, never run), and MinIO had no restore code or documentation at all.

- Added [`omniflow-deploy/restore.sh`](omniflow-deploy/restore.sh): real, runnable restore for both — `restore.sh qdrant <collection> <snapshot-file>` (copies the snapshot into the container, calls Qdrant's real recover API) and `restore.sh minio <backup-prefix> <destination-bucket>` (a real `mc mirror` in the opposite direction from backup.sh's). Uses the exact same container/network env-var conventions as `backup.sh` so a restore always targets the stack a backup was taken from.
- Updated `backup.sh`'s inline comments to point at the new real script instead of leaving restore as unverified prose.
- **Verified against the real local dev stack** (real Qdrant, real MinIO — not mocks): new [`omniflow-deploy/tests/restore_drill.sh`](omniflow-deploy/tests/restore_drill.sh), ran twice. Qdrant: creates a real collection with 3 points, snapshots it, **deletes the collection entirely** (confirmed 404 before restoring), restores via the real `restore.sh`, and confirms all 3 points are back (`points_count` verified, not just an HTTP 200 from the recover call). MinIO: creates a real bucket with 2 objects, mirrors to a backup bucket, **deletes the source objects entirely** (confirmed empty), restores via `restore.sh`, and diffs the restored files byte-for-byte against the originals (not just an object-count check).
- Real bug found and fixed by the drill itself, not assumed away: on Windows, `docker cp`/`docker run -v` arguments that look like absolute paths get silently mangled by Git Bash's MSYS path translation, pointing restore operations at the wrong directory. Fixed with an `MSYS_NO_PATHCONV=1` export and a `cygpath`-based path-translation helper — a no-op on the Linux hosts this actually deploys to.

**Not covered**: PostgreSQL's restore drill was already done in an earlier session (per `RELEASE_CHECKLIST.md`) and isn't re-tested here. A production run of `restore.sh` still assumes the same trusted operator context as `backup.sh` (direct Docker access to the deployment host) — no additional access-control hardening was added.

### Items 21–24: audited, all genuinely blocked on external input — 2026-09-24

- **21 (production domain/TLS/provider keys)**: no domain, no TLS cert, no production Moyasar/Tap/Meta credentials exist anywhere in this repo. Nothing to build without them.
- **22 (refreshed SRS-vs-code review)**: the item itself requires "explicit sign-off from the user on what's in vs. explicitly deferred for v1" — this is a collaborative decision-making item, not a coding task. The individual scope decisions it would cover (voice/image, VIP tiering, channel scope) have already been made and documented per-item throughout this pass; a single consolidated re-review is best done as one pass over the now-updated `IMPLEMENTATION_STATUS.md` with the user, not invented unilaterally here.
- **23 (staging acceptance/load tests)**: transitively blocked — needs a staging environment, which needs the domain/hosting from item 21.
- **24 (privacy policy/terms/support channel)**: not drafted. A privacy policy/ToS needs real business facts this session doesn't have and shouldn't guess — legal entity name, registered address, jurisdiction, a real DPO/support contact. Saudi PDPL and consumer-protection requirements make guessed placeholders for these specifically risky to ship even as a "draft," unlike a code default that's easy to flag and revisit. Deferred until those facts are provided, rather than fabricated.

### Sign-off follow-ups on items 7, 8, 10, 11 — 2026-09-24

The user reviewed the open sign-off questions flagged in items 7/8/10/11 and made four decisions, implemented here.

**Item 7 — tenant contact number stored as full E.164, defaulting to Saudi.** Decision: store `Tenant.whatsapp_display_phone_number` as a complete international E.164 number, not a bare local one — validate it starts with a real country code, default-assume Saudi (+966) when the admin types a number with no code, but never block a number that already has its own country code (brokerages can have non-Saudi-resident customers).
- `gateway/routers/settings.py`: `PATCH /api/v1/settings` now accepts `whatsapp_display_phone_number` (previously nothing in the product captured this field at all, per item 7's original gap). Normalization uses the already-declared-but-previously-unused `phonenumbers` dependency (Google's libphonenumber) with `region="SA"` — this is exactly the "default to Saudi when no code, respect an explicit code otherwise" behavior, verified empirically: `"0501234567"` → `+966501234567`, `"+201001234567"` stays `+201001234567` unchanged, garbage input is rejected by the validator (a real 422 under FastAPI's request cycle).
- **Verified with real Postgres**, through the actual `update_settings_endpoint` function with a real `Tenant`/`TenantUser` row. New script: [`scripts/validate_tenant_phone_e164.py`](omniflow-backend/scripts/validate_tenant_phone_e164.py), ran twice: local-format normalization, foreign-country-code preservation, invalid-input rejection, and durable persistence (re-read in a fresh session) all confirmed.

**Item 8 — VIP tiering confirmed as-is, no change needed.** Decision: keep it simple for v1 — a single `is_vip` flag, campaigns target "all" or "VIP only," no Gold/Platinum-style tier system until an actual customer asks for one. Audited `gateway/routers/broadcasts.py` and confirmed the existing implementation (built in the original item 8 pass) already matches this exactly: `Customer.is_vip` is the only signal, `_normalise_segment()` only accepts `all | vip | engaged | new | inactive`, and `list_type` is accepted merely as a legacy input-key alias, not a real tiering column. No code change required — this decision closes the open sign-off question without altering behavior.

**Item 10 — explicit LLM preference extraction now runs before search, with real price/location filtering.** Decision: extract explicit preferences (budget, district, property type, bedrooms) via an LLM from the customer's recent messages, instead of embedding the raw message text as-is — more accurate for real estate since price/location matter more than lexical similarity, and cheaper in tokens.
- New [`ai_workers/rag_engine/preference_extractor.py`](omniflow-backend/src/ai_workers/rag_engine/preference_extractor.py): a single low-temperature JSON-mode LLM call (same pattern as `llm_orchestrator.py`'s existing `_classify_intent`) extracts `{budget_min, budget_max, district, property_type, bedrooms}`. Fails closed to "nothing extracted" on any error, so the endpoint always has a safe fallback.
- `shared/qdrant_client/client.py`: new `build_preference_filter()` + `search_properties(..., extra_filters=...)` — price/district/property_type/bedrooms are now real Qdrant filter conditions, not just text folded into the embedding.
- `gateway/routers/conversations.py`'s `get_conversation_recommendations`: builds a canonical query from extracted preferences when available (falls back to the old raw-message-join behavior when nothing is confidently extracted), and searches in three relaxation stages — full filter (price + district + type + bedrooms) → price-only filter → no filter — so a district string mismatch (e.g. the LLM extracting "حي النرجس" against a listing stored as just "النرجس") can't zero out the panel; it only relaxes down to the price filter, which is exact numeric comparison and doesn't have that brittleness.
- **Verified with a real LLM call (Groq, a real configured provider), real Postgres, real Qdrant, real fastembed** — no mocks anywhere in this pipeline. New script: [`scripts/validate_recommendation_preferences.py`](omniflow-backend/scripts/validate_recommendation_preferences.py), ran twice. Scenario A: the real LLM correctly extracts `property_type=apartment, bedrooms=3, district≈النرجس, budget_max≈1.2M` from a realistic Arabic customer message. Scenario B: given one listing priced within budget (1.15M SAR) and a near-identical-text listing priced at 4.5M SAR (deliberately chosen to have HIGH raw text similarity but fail the price filter), the real endpoint returns the in-budget listing and correctly excludes the over-budget one — proving the filter actually constrains results, not just re-ranks them. Confirmed reproducible across both runs even though the LLM's exact district string varied ("النرجس" vs "حي النرجس" between runs) — the relaxation logic handled both correctly.
- **Not covered**: no A/B evaluation of extraction accuracy across a broader message corpus; `bedrooms`/`property_type` filters were not separately stress-tested for their own string/enum-mismatch risk the way `district` was (property_type is a controlled enum sent by the LLM prompt, lower risk; bedrooms is numeric, no mismatch risk).

**Item 11 — appointments scope cut confirmed as-is, no change needed.** Decision: the existing minimal date/time + free-text location note is acceptable for v1; the SRS's full calendar-sync/distance-routing/reminder subsystem is explicitly deferred, to be revisited only if a client actually requests real calendar integration. No code change — this closes the open sign-off question. Recorded here as a known, accepted limitation rather than an open question.

### Naeem test tenant: onboarding never collected business data, payment gate audit, new client direction — 2026-09-25

The client asked to defer payments (item 3) entirely and instead onboard "نعيم" (Naeem — a home-wellness/massage-device retailer, deliberately non-real-estate) as the first real internal test tenant, driven through the actual onboarding flow rather than seeded into the DB. Auditing that flow surfaced a real, previously undocumented gap.

**Newly discovered gap: onboarding never collected business data at all, and the AI shipped a hardcoded wrong persona by default.** `PATCH /api/v1/tenants/onboarding` (`gateway/routers/tenants.py`) and its frontend page (`onboarding/page.tsx`) only ever handled the WhatsApp/Meta channel-linking step (`option`, `meta_access_token`, `whatsapp_phone_number_id`, `whatsapp_waba_id`). There was no field anywhere in onboarding for business name, category, description, or products — a brand-new tenant's `business_name` stayed whatever the Clerk-webhook auto-provisioner guessed (`"Workspace for {clerk name}"`), and the AI-facing company-knowledge system (`ai_engine/company_context.py`, which builds the block injected into every LLM call) was fed exclusively by a completely separate, disconnected dashboard screen (`PATCH /api/v1/knowledge/profile`) that nothing in onboarding pointed a new tenant toward. Worse: `llm_orchestrator.py`'s default persona is hardcoded to *"أنت المستشار أحمد الصائغ ... مستشار عقاري سعودي محترف"* (a Saudi real-estate consultant) — actively wrong and confusing for any tenant, like Naeem, that isn't in real estate, and nothing in the tenant-creation path ever overrode it.

Fixed, not just documented:
- `TenantOnboardingUpdate` gained `business_name`, `business_category`, `about_text`, `products` — all optional, all wired into the real onboarding submission.
- `update_tenant_onboarding` now writes `Tenant.business_name`, upserts `CompanyProfile.business_description`/`services_offered` (category + products), and — only when no admin has already customized one — generates a vertical-neutral starting persona from the submitted business name/category, replacing the hardcoded real-estate default for any tenant that fills this in.
- **Real bug caught while implementing, not shipped**: `CompanyProfile` is a real RLS-protected table; the endpoint's existing `Tenant` write uses a bare `AsyncSessionFactory()` session with no RLS tenant context (correct for `tenants`, which is the RLS partition key itself and deliberately unprotected — but wrong for `company_profiles`). Fixed by moving that write into its own `get_tenant_session(...)` block, which injects the RLS context properly. Caught before shipping because the validation script actually exercises this path against real Postgres rather than assuming an ORM `session.add()` "just works" regardless of which session it's called on.
- Frontend: added a shared "عن شركتك" (about your business) section above both onboarding paths — business name (required), category, about text, and a comma-separated product list — submitted with whichever WhatsApp option the tenant picks. `tsc --noEmit`, `eslint` (only pre-existing warnings), and a full production build all pass.
- **Verified against real Postgres**, through the actual `update_tenant_onboarding` endpoint function (not a reimplementation): new script [`scripts/validate_onboarding_business_profile.py`](omniflow-backend/scripts/validate_onboarding_business_profile.py), ran twice. Confirms `Tenant.business_name` actually changes (not left as the placeholder), the generated persona is vertical-neutral and does NOT contain the real-estate default's tell (`"عقاري"`), and — the actual point of the test — the real `get_company_context()` function (the exact one the AI hot path calls on every reply) returns a block containing the submitted business name, category, and products.
- **Not covered**: no generic structured product/catalog table exists (confirmed via audit) — a retailer's inventory is represented as prose in `CompanyProfile.services_offered`/`business_description`, or as an uploaded document via the existing Knowledge Base (S3 → Qdrant RAG), not as priced/stocked SKU rows. This is sufficient for Naeem's short "known products" list but would not scale to a large structured catalog without further work, which was out of scope for this fix.

**Real-estate-specific feature isolation — confirmed clean, no change needed.** Audited whether REGA verification / property recommendations / the semantic router would misfire for a non-real-estate tenant. `PropertyListing`-backed endpoints are naturally inert with zero rows (empty results, no errors); REGA "verification" is a no-op placeholder regardless of vertical (confirmed in the item-15 audit above) so it neither helps nor breaks anything for Naeem. **One real limitation found and left as a documented gap, not fixed**: `semantic_router/classifier.py`'s L2/L3 triggers are hardcoded to real-estate vocabulary (شقة/فيلا/عقار/صك, etc.) — a massage-device customer's messages will always fall through to the generic L1 tier rather than misfiring into wrong real-estate behavior (safe), but this also means Naeem gets zero benefit from L2's RAG-document retrieval even if a future product catalog is uploaded as a `KnowledgeDocument`. `CompanyProfile`'s always-injected block (fixed above) is unaffected by this and covers Naeem's current small "known products" list, so this doesn't block the immediate test — flagged for whoever eventually makes the classifier vertical-agnostic.

**Payment gating (item 3) — audited and confirmed there is no functioning gate to bypass.** Checked every place `Tenant.status`/`SubscriptionStatus` could plausibly block usage: `status` defaults to `TRIAL` at signup (`webhooks.py`'s Clerk-triggered auto-provisioner) and is **never transitioned anywhere in the backend** — no billing webhook handler exists that flips it. The one real enforcement point (`semantic_router/tenant_resolver.py` filtering `status IN (active, trial)`) is therefore dead code in practice: it enforces a state (`past_due`/`suspended`/`cancelled`) the system never enters. `ai_gateway.py`'s `_verify_tenant_context()` is an explicit `[STUB — Phase 2]` that always returns `True`, with the real suspension check commented out. **Conclusion: no bypass was implemented because there is nothing to bypass** — any tenant, Naeem included, already gets full, ungated usage under the default `TRIAL` status. Per the client's ask to "flag it as free/internal in the least invasive way": rather than add a new schema column for a gate that doesn't exist yet (unnecessary churn), the convention is recorded here explicitly for whoever eventually builds real billing: **`TRIAL`-status tenants must remain exempt from any new payment gate until explicitly migrated to a paid tier** — Naeem should stay on `TRIAL` indefinitely as its "internal/free" marker, not be given a special new flag, since `TRIAL` already is, and will remain, the free/ungated state.

### Naeem registered as a real tenant through the real onboarding flow, end-to-end — 2026-09-25

Following on from the onboarding-gap fix above, actually drove the onboarding a real company would use — not seeded into the DB.

- Extended the business-profile step further per live feedback while testing: added `detailed_instructions` (→ `CompanyProfile.policies_text`, a larger free-text field for policies/FAQ/how-to-answer guidance) and a real file-upload control in the onboarding UI wired to the **already-existing** Knowledge Base pipeline (`POST /api/v1/knowledge/documents` — real S3 upload → Celery → chunk/embed → Qdrant RAG), so a company can attach a catalogue/price list/FAQ document during onboarding itself instead of discovering the separate dashboard screen later. Files upload automatically right after the WhatsApp-linking step succeeds; a failed file upload is reported but never blocks finishing onboarding, since the tenant is already fully onboarded by that point.
- **Real, unrelated bug found and fixed while trying to run the local dev server for this test**: `tailwind.config.ts` used `require("tailwindcss/plugin")` inside a `.ts` config file — this crashed `next dev --webpack` outright (`ReferenceError: require is not defined`) even though `next build` was unaffected (different module-loading path). Fixed with a proper `import plugin from "tailwindcss/plugin"`. This had been silently blocking local frontend development entirely; not discovered until actually trying to run the dev server for this test.
- **Registered a real Clerk user and drove the real onboarding API end-to-end** — not a synthetic drill, the actual account: created via Clerk's Backend API (`naeem.omniflow.test@example.com`, tenant_id `e3699aa1-6a4e-4cde-bb26-b62db0b95d23`), authenticated with a real Clerk session JWT obtained via Clerk's sign-in-token exchange (used because the local dev Clerk instance's Cloudflare Turnstile bot-check returns error 600010 — Turnstile's own "domain not registered" error, not a bot-detection failure — for `localhost`; this needs a Clerk-dashboard fix, i.e. registering `localhost` or disabling bot protection for the dev instance, which is outside what this session can access). Submitted the real business data (name, category, about text, products) through the real `PATCH /api/v1/tenants/onboarding` endpoint.
- **Real bug caught in my own tooling, not the product**: the first submission (made via a Git-Bash `curl` command with literal Arabic text in the command-line argument) silently corrupted the Arabic text to `????` before it ever left the shell — a Windows console-encoding artifact of passing multi-byte UTF-8 through `curl.exe`'s argument boundary, not a bug in the endpoint (already proven correct by the synthetic validator's real-Arabic assertions). Recognized because a direct DB read (via Python, bypassing the shell argument boundary) showed the same corrupted value, ruling out a display-only artifact. Resubmitted via a Python `httpx` call instead (no shell argument encoding involved) and confirmed clean Arabic end-to-end. The persona field needed a manual one-line fix afterward, because the endpoint's own "never clobber an already-set persona" safeguard (correct, protects real admin customizations) was — correctly — protecting the corrupted value from my first attempt; not a product bug either.
- **Final verification — the actual point of this whole exercise**: generated a real AI reply from `LLMOrchestrator.generate_response()` for the customer message *"عندكم جهاز مساج للركبة؟ وايه الفرق بينه وبين جهاز القدم؟"* using Naeem's real, onboarded persona and company-knowledge block. The AI correctly identified itself as being from **نعيم**, discussed the **actual** foot and knee massage products from the onboarding data (with no product data invented), referenced the "quality and warranty" language from the submitted about-text, and never once mentioned real estate. This is the concrete, end-to-end proof that onboarding data reaches — and correctly shapes — a real customer-facing AI reply for a non-real-estate tenant.
- **Still needed from the client before this tenant is fully live**: real WhatsApp/Meta credentials (currently placeholder `PENDING` values) to replace the item-3 WhatsApp answer already given, and, separately, fixing the Clerk dashboard's Turnstile/bot-protection config for `localhost` so future local sign-ups don't need the Backend-API workaround used here.

### Real live test caught a real gap: Meta status webhooks were silently dropped — 2026-09-25

While validating Naeem's real WhatsApp connection end-to-end (real Meta token, real Phone Number ID/WABA ID, real ngrok tunnel, real webhook registered and verified by Meta's own servers), sent a real outbound message via the actual `WhatsAppClient` code. The Graph API accepted it (200, a real wamid) but it never arrived at the test phone. The client correctly suspected the WhatsApp 24h customer-service-window rule (no prior inbound message from that number, so a free-form outbound send is accepted by the API but fails delivery) — confirmed by checking real webhook traffic in the backend logs: Meta's real status-callback webhook for that exact wamid *did* arrive (a real `POST /api/v1/webhooks/whatsapp` from a genuine Meta IP), but produced no visible effect anywhere.

**Root cause, confirmed by reading the code, not guessed**: `channel_adapters/whatsapp/router.py`'s `_process_whatsapp_value()` — the exact function every real webhook POST is routed to — only ever read `value["messages"]`. `value["statuses"]` (Meta's real sent/delivered/read/failed events, including the error code+title on failure) was never touched anywhere. This is the same gap items 8 and 12 had already documented in general terms ("the Meta status-callback webhook... does not exist anywhere in this codebase") — this real test is what proved it concretely, with a real error code, not a theoretical gap. The client's specific ask — "does the message record in our DB actually show failed with a reason, or does it silently sit as sent forever" — the answer was confirmed to be the latter.

**Fixed**:
- New migration [`0012_msg_failure_reason`](omniflow-backend/alembic/versions/0012_message_failure_reason.py): `messages.failure_reason` (nullable text), applied to the real local DB.
- New `persistence.update_message_delivery_status_by_wamid()` — looks up a message by `platform_message_id` (the wamid; the webhook never gives us our own internal `message_id`), updates `delivery_status` + `failure_reason`, publishes the existing `message_status_update` SSE event (item 12's mechanism, now actually fed by real status data instead of only ever reaching SENT).
- `_process_whatsapp_value()` now processes `value["statuses"]` for real: extracts `id`/`status`/`errors[0]` per Meta's documented real webhook shape, calls the above function. Logs (not raises) on any per-status error, consistent with the existing per-message error handling in the same function — Meta must never see a non-200 from this endpoint.
- **Verified against real Postgres**, through the actual `_process_whatsapp_value()` function (not a reimplementation) with a real persisted outbound message row and a status payload shaped exactly like Meta's real "failed" webhook (error 131047, the exact real error this session hit). New script [`scripts/validate_whatsapp_status_webhook.py`](omniflow-backend/scripts/validate_whatsapp_status_webhook.py), ran twice: the message's `delivery_status` flips from `SENT` to `FAILED` and `failure_reason` correctly contains `"131047: Re-engagement message"`.
- Also fixed, found during the same live session: a real duplicate-key bug in `.env` (two conflicting `META_WEBHOOK_HMAC_SECRET` values — the second silently shadowed the first, per pydantic-settings' last-value-wins `.env` parsing); and a real, confirmed-broken self-service UI (`settings/page.tsx`'s "Phone Number ID" field was editable and submittable but the backend's `TenantSettingsPatch` schema silently dropped it — a save reported success but never persisted). Fixed by making it read-only with a link back to onboarding, consistent with how Meta Access Token already behaved correctly; the real, only-working path for updating any WhatsApp credential remains `PATCH /api/v1/tenants/onboarding` (confirmed re-callable anytime, not gated to first use).
- **Naeem's real WhatsApp connection is now fully live**: real Meta token/Phone Number ID/WABA ID stored via the real onboarding endpoint, confirmed against the real Graph API (`GET /{phone_number_id}` returned `verified_name: "Test Number"`, `quality_rating: GREEN`), webhook registered via the Graph API's app-subscriptions endpoint and independently confirmed by a real verification handshake from Meta's own servers in the backend logs.
- **Not covered**: the failure_reason isn't yet surfaced in the frontend's `DeliveryStatusIcon` tooltip (item 12's UI) — the data now reaches the DB and the SSE event correctly, but no UI reads `failure_reason` yet. Left as a follow-up, out of scope for what this live test was actually checking.

### Real inbound test traced to two more real bugs in the VCard send path — 2026-09-25

The client sent a real inbound WhatsApp message ("عندكم مساج للركبة") and got no reply. Traced through every stage with real logs, per their explicit request, rather than guessing:

1. **Webhook receipt**: real POST from a genuine Meta IP, correctly resolved to Naeem's tenant, correctly persisted (new customer + new conversation created), correctly published to `messages.incoming.v1` (offset 13). No issue here.
2. **semantic_router**: consumed it, but — correctly, by design — intercepted it as `vcard_gatekeeper_interception` (`target_tier=VCARD, skip_llm=True`) because this was the customer's first-ever message (`vcard_state=STATE_NEW`, item 7's VCard Gatekeeper). Published to `vcard.gatekeeper.v1`. **Not a bug**: this deliberately routes a brand-new customer to receive a VCard contact card before their question is answered by the AI — this session's earlier item-7 work.
3. **The actual failure**: nothing was consuming `vcard.gatekeeper.v1` at all — the `vcard_gatekeeper` worker process wasn't running (an operational gap in this session's manual multi-worker setup, not a code bug: only semantic_router/llm_invoker/outbound_dispatcher had been started). Fixed by starting it; it then correctly built and queued a real VCard send.
4. **A real, confirmed bug in `WhatsAppClient`**: the VCard send then failed for real, with Meta's own error `(#100) The parameter messaging_product is required`. Traced to the exact line by testing `upload_media()` and `send_document_message()` independently against the real Graph API: `upload_media()`'s multipart upload was the failure — `WhatsAppClient.start()` set a **client-level default `Content-Type: application/json` header**, which this httpx version applies even to `files=` (multipart) requests, silently overriding the auto-generated `multipart/form-data; boundary=...` the upload actually needs. The code's own existing comment ("httpx overrides the client's default... automatically — no manual header surgery needed") was empirically wrong — confirmed by inspecting `request.headers` on the actual outgoing request, not assumed. Fixed by removing the client-level default entirely; httpx correctly auto-sets the right Content-Type per-request for both `json=` and `files=` calls once nothing overrides it.
5. **A second real bug, found immediately after fixing the first**: with the Content-Type fixed, Meta returned a *different*, real, documented error — `text/vcard` is not in Meta's Media API's accepted-MIME-type whitelist at all (confirmed via the full error message: only `audio/*`, `video/mp4`, `video/3gpp`, Office formats, `application/pdf`, `text/plain`, and a few image types are accepted). Fixed by uploading with `mime_type="text/plain"` instead — WhatsApp still renders it as a contact card client-side based on the `.vcf` filename, not the upload-time MIME type.
- **Verified against the real Meta Graph API directly** (not a mock, not a reimplementation): both the broken and fixed states were reproduced live — confirmed the exact wrong Content-Type being sent, confirmed the fix produces the correct multipart Content-Type, then ran the real `upload_media()` + `send_document_message()` calls through the actual `WhatsAppClient` singleton end-to-end and got a real `200`/wamid back from Meta.
- **This is exactly the class of gap item 6's crash-testing bar was meant to catch, and didn't** — the earlier item-7 VCard validation (`validate_vcard_delivery.py`) used `httpx.MockTransport`, which faithfully exercises the client's request-building code but never validates against Meta's real, strict multipart-parsing and MIME-whitelist behavior. Noting this honestly rather than implying the earlier mock-transport test should have caught it: it was scoped to prove the request-building logic, not real-server compatibility, and that scope limit is exactly what let both of these through.
- **Not yet re-verified**: the fix has been proven against the real Graph API directly (raw client calls), but not yet through a fresh real inbound message replaying the full `vcard_gatekeeper` → `outbound_dispatcher` pipeline end-to-end (the original failed job already exhausted its retries to DLQ before the fix landed). The next real customer message will exercise the fixed code path for real.

### VCard content was nearly empty — fixed, verified on a real device — 2026-09-25

The client actually opened the delivered VCard and found it real but nearly useless: `FN:Naeem` only, no phone number, no other business info — a contact card with no phone number defeats the feature's purpose.

- **Root cause**: `vcard_builder.build_tenant_vcard()` always supported a `phone`/`TEL` field, but the caller (`outbound_dispatcher/worker.py`) only ever passed `business_name` and `Tenant.whatsapp_display_phone_number` — and Naeem's tenant had never had that field set (a separate field from the WhatsApp Cloud API's `whatsapp_phone_number_id`, per item 7). Not a code bug in the builder — the input was genuinely empty.
- **Extended `build_tenant_vcard()`** with `category`/`note`/`email`/`website`, all optional, and updated the caller to fetch `CompanyProfile` alongside `Tenant` and pass through `services_offered[0]` (category) → `ORG`, `business_description` → `NOTE`, `contact_email` → `EMAIL`, `social_links.website` → `URL`. A tenant with no `CompanyProfile` row still gets exactly the old FN-only behavior — nothing regresses for a tenant that hasn't filled in onboarding's business-profile step.
- **A real, legitimate edge case hit while setting Naeem's own display phone**: the Meta-assigned WhatsApp test number is `+1 555-152-8402` — the North American numbering plan's `555` prefix is deliberately reserved for fictional/non-dialable numbers (movies, test systems), so item 7's own E.164 validator correctly rejected it as genuinely not a real, callable number. This is not a validator bug — it's an inherent property of Meta's test numbers. Documented here rather than silently worked around: production tenants with a real WhatsApp Business number won't hit this; Naeem's test tenant has the sandbox number set directly (bypassing the validator, since it's a test fixture, not real customer-facing data) purely so the TEL-field *mechanism* could be verified end-to-end.
- **Verified for real, per the client's explicit ask "open the .vcf on a phone and confirm a working phone number, not just a name"**: built the real VCard via the actual production code path (`Tenant` + `CompanyProfile` from real Postgres, same code `outbound_dispatcher` runs), parsed it with `vobject` to confirm `FN`/`ORG`/`TEL`/`NOTE` all round-trip correctly, then sent it through the real, now-fixed `WhatsAppClient` (`upload_media` + `send_document_message`) to the client's real phone — real `200`/wamid back from Meta. The resulting card now has: FN=نعيم, ORG="نعيم — أجهزة مساج احترافية", TEL=+15551528402 (test number), NOTE=the full onboarded about-text.
- **Also traced (per the client's separate question)**: their follow-up text message ("عندكم مساج للركبة؟", meant to complete the round trip) never reached our webhook at all. Confirmed via ngrok's own independent request log (not our app — verifies delivery attempts regardless of backend state): exactly 8 POSTs have ever hit the tunnel since it was created, all `200 OK`, none failed or dropped, none of them a second inbound text message. Not stuck, not failed — never sent to us. Reported back rather than guessed at; everything (tunnel, webhook subscription, all 4 workers) was independently confirmed live and stable at the time.

## Next steps and known gaps

1. Real database inbox/report integration and synthetic Kafka/Redis transport checks pass. Next validate the authenticated browser journey and the combined worker flow; the inbox validator still mocks transport/channel boundaries.
2. Validate the pending-message publisher with the real database and Kafka. Review recovery of QUEUED/FAILED messages and DLQ operations. Delivery cannot promise exactly-once across a crash between external API acceptance and durable acknowledgement.
3. SSE reconnect/cancellation and list/history refresh are implemented and unit-tested. Still validate them in an authenticated browser, complete message-history pagination for long conversations, and implement delivery-status UI/SSE updates.
4. Shared chat provider wiring is complete and live-tested with Groq. Complete real embedding/retrieval support and verify tenant isolation in the Instagram path.
5. Complete real VCard contact sending and missing production worker wiring. Current VCard state changes do not prove a contact was delivered.
6. Replace report/PDF, broadcast, payment, and other placeholder paths with the behavior required by the SRS. Implement and verify the remaining channels and product journeys.
7. Produce immutable release images and validate the complete staging stack, off-host backups, Qdrant/MinIO restore, public storage and observability. Both local Docker verification images contain the final source; the PostgreSQL restore drill passed and development infrastructure checks are healthy. This does not establish complete production-stack readiness.
8. Concurrency audit complete (item 13): conversation-counter and takeover races fixed with atomic SQL, SSE-before-commit fixed via BackgroundTasks, user provisioning already safe. Remaining open risk: the AI-reply-after-takeover window in `outbound_dispatcher` is narrowed (a second pre-send check) but not fully closed — a real fix needs a lock/fencing token on the conversation.

The product is not complete. Passing unit tests and a frontend build must not be presented as end-to-end or production verification.
